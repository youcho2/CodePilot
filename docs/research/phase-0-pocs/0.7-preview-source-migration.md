# Phase 0.7 POC — PreviewPanel 迁移到 PreviewSource discriminated union

> 对应执行计划：[`docs/exec-plans/active/markdown-artifact-overhaul.md`](../../exec-plans/active/markdown-artifact-overhaul.md) Phase 1.5
>
> 本 POC 为只读调研，不改任何源码。目的：为 Phase 1.5 的数据模型迁移提供一份全仓引用清单 + 类型草案 + 迁移策略，确保 inline-html / inline-jsx / inline-datatable 三种 kind 在 Phase 2 / 5.4 落地时不会因为 PanelZone gate 漏改而挂不上面板。

---

## [现状]

### 1. Context 定义（`src/hooks/usePanel.ts`）

当前 `PanelContextValue` 只有 file-based 的预览 API：

- `src/hooks/usePanel.ts:47` — `previewFile: string | null`
- `src/hooks/usePanel.ts:48` — `setPreviewFile: (path: string | null) => void`
- `src/hooks/usePanel.ts:15-16` — `previewOpen` / `setPreviewOpen` 是独立的 boolean，不归属 `previewFile` 类型
- `src/hooks/usePanel.ts:7` — `export type PreviewViewMode = "source" | "rendered"`
- `src/hooks/usePanel.ts:49-50` — `previewViewMode` / `setPreviewViewMode`

两个面板状态维度：
1. **内容维度** — 当前只有 `previewFile: string | null`，只支持文件路径
2. **可见性维度** — `previewOpen: boolean`，独立于内容（可以 `open=true, file=null`）

### 2. 状态所有者（`src/components/layout/AppShell.tsx`）

`AppShell` 是唯一的状态生产者，通过 `useMemo` 装配成 Context value：

- `src/components/layout/AppShell.tsx:177` — `const [previewOpen, setPreviewOpen] = useState(false);`
- `src/components/layout/AppShell.tsx:355` — `const [previewFile, setPreviewFileRaw] = useState<string | null>(null);`
- `src/components/layout/AppShell.tsx:356` — `const [previewViewMode, setPreviewViewMode] = useState<PreviewViewMode>("source");`
- `src/components/layout/AppShell.tsx:358-366` — `setPreviewFile` 封装版：设置 raw 值 + 副作用（自动设 `previewViewMode` + `setPreviewOpen(true/false)`）
  ```ts
  const setPreviewFile = useCallback((path: string | null) => {
    setPreviewFileRaw(path);
    if (path) {
      setPreviewViewMode(defaultViewMode(path));
      setPreviewOpen(true);
    } else {
      setPreviewOpen(false);
    }
  }, []);
  ```
- `src/components/layout/AppShell.tsx:66-70` — `defaultViewMode(filePath)` 基于后缀决定 `"rendered"` / `"source"`
- `src/components/layout/AppShell.tsx:370-373` — 路由切换时 effect 自动清空 `previewFile`/`previewOpen`
- `src/components/layout/AppShell.tsx:415-452` — Context value 的 `useMemo` 打包
- `src/components/layout/AppShell.tsx:447-450` — 暴露字段：`previewFile`, `setPreviewFile`, `previewViewMode`, `setPreviewViewMode`

### 3. `PanelZone.tsx` gate（关键阻塞点）

- `src/components/layout/PanelZone.tsx:13` — 解构 `previewOpen, previewFile`
- `src/components/layout/PanelZone.tsx:15` — `const anyOpen = (previewOpen && !!previewFile) || gitPanelOpen || fileTreeOpen || dashboardPanelOpen || assistantPanelOpen;`
- `src/components/layout/PanelZone.tsx:22` — `{previewOpen && previewFile && <PreviewPanel />}`

这两处是 Phase 1.5 的**必改点**。inline-* 三种 kind 没有 `filePath`，如果 gate 仍按 `!!previewFile` 判断，面板永远挂不上。

### 4. `PreviewPanel.tsx` 消费端

- `src/components/layout/panels/PreviewPanel.tsx:90` — 解构 `previewFile, setPreviewFile, previewViewMode, setPreviewViewMode, setPreviewOpen`
- `src/components/layout/panels/PreviewPanel.tsx:103` — `const filePath = previewFile || "";` 整个组件只用 `filePath` 这个字符串
- `src/components/layout/panels/PreviewPanel.tsx:105-142` — `useEffect(loadPreview)` 基于 `filePath` 发 `/api/files/preview` 请求；inline 场景需要跳过
- `src/components/layout/panels/PreviewPanel.tsx:144-149` — `handleCopyContent` 从 `preview.content || filePath` 取内容
- `src/components/layout/panels/PreviewPanel.tsx:151-154` — `handleClose` 同时调 `setPreviewFile(null)` + `setPreviewOpen(false)`
- `src/components/layout/panels/PreviewPanel.tsx:156` — `fileName = filePath.split("/").pop() || filePath`（inline 需要 `virtualName` 兜底）
- `src/components/layout/panels/PreviewPanel.tsx:158-163` — `breadcrumb` 完全基于 `filePath` split
- `src/components/layout/panels/PreviewPanel.tsx:165-175` — `canRender`/`isMedia`/`fileServeUrl` 全部基于 `filePath` 判断
- `src/components/layout/panels/PreviewPanel.tsx:186-238` — 渲染分支判断：`isMedia` → MediaView、`canRender && rendered` → RenderedView、else → SourceView
- `src/components/layout/panels/PreviewPanel.tsx:222-234` — `MediaView` / `RenderedView` / `SourceView` 都接收 `filePath` prop 作为副参数（扩展名判断等）

### 5. 其他触点

- `src/components/layout/panels/FileTreePanel.tsx:18` — 解构 `previewFile, setPreviewFile, setPreviewOpen`
- `src/components/layout/panels/FileTreePanel.tsx:48-55` — `handleFileSelect` toggle 逻辑：`if (previewFile === path) { setPreviewFile(null); setPreviewOpen(false); } else { setPreviewFile(path); setPreviewOpen(true); }`

`UnifiedTopBar.tsx` (`src/components/layout/UnifiedTopBar.tsx:26-43`) 虽 `usePanel()`，但**不消费** `previewFile` / `setPreviewFile` / `previewOpen` — 仅用 fileTree/git/dashboard/assistant 面板开关。`ChatView` / `ChatListPanel` / `GitPanel` 等同理不碰预览字段。全仓 grep 确认：**除了上述 5 个文件（`usePanel.ts`、`AppShell.tsx`、`PanelZone.tsx`、`PreviewPanel.tsx`、`FileTreePanel.tsx`）外，无其他 `previewFile` / `setPreviewFile` 消费点**。

### 6. 约束文件（产物 `setPreviewSource` 调用方预计落地在）

执行计划（`docs/exec-plans/active/markdown-artifact-overhaul.md:442`）预留的调用位置示例：`setPreviewSource({ kind: 'file', filePath: file.path })` 用于 Artifact 卡片点击；Phase 5.4（`:858`）则用 `kind: 'inline-datatable'`。这些是 Phase 1.5 之后的工作，不在本 POC 范围。

---

## [设计]

### PreviewSource discriminated union（完整 TS 类型）

建议直接写进 `src/hooks/usePanel.ts`（或独立 `src/types/preview.ts` 再 re-export，取决于是否想让非 panel 模块也复用；本 POC 推荐就放在 `usePanel.ts` 以减少文件新增噪音）：

```ts
/**
 * 预览面板的内容来源 — discriminated union。
 *
 * - `file`:            基于工作目录内文件路径，由 /api/files/preview 拉取
 * - `inline-html`:     直接传入 HTML 字符串，走 iframe srcDoc 渲染（Phase 2.1）
 * - `inline-jsx`:      直接传入 JSX/TSX 源码，走 Sandpack 渲染（Phase 2.1 / 2.2）
 * - `inline-datatable`: 结构化表格数据，走 DataTable viewer（Phase 5.4）
 *
 * 所有 inline-* 变体可选带 `virtualName`，用于 header 标题 / breadcrumb / copy 的兜底文本。
 */
export type PreviewSource =
  | { kind: 'file'; filePath: string }
  | { kind: 'inline-html'; html: string; virtualName?: string }
  | {
      kind: 'inline-jsx';
      jsx: string;
      virtualName?: string;
      /** 可选 runtime 绑定，Phase 2.1 Sandpack 决定是否注入 */
      bindings?: Record<string, unknown>;
    }
  | {
      kind: 'inline-datatable';
      rows: unknown[][];
      header: string[];
      virtualName?: string;
    };
```

### `PanelContextValue` 改动

在 `src/hooks/usePanel.ts` 的 `PanelContextValue` 中，替换 L47-48：

```ts
// 主字段 — 新功能必须走这个
previewSource: PreviewSource | null;
setPreviewSource: (source: PreviewSource | null) => void;

// 兼容旧 API 的 adapter — 保留现有 FileTreePanel / 旧代码路径零回归
// previewFile 是 previewSource 的派生视图：仅当 kind === 'file' 时才非 null
previewFile: string | null;
setPreviewFile: (path: string | null) => void;
```

### `AppShell` adapter 代码草案

替换 `src/components/layout/AppShell.tsx:355-366` 的实现。核心思路：**state 唯一来源变为 `previewSource`**，`previewFile` / `setPreviewFile` 是派生视图 + 包装函数，保持外部 API 兼容。

```ts
// 单一 state（替代原 previewFile useState）
const [previewSource, setPreviewSourceRaw] = useState<PreviewSource | null>(null);
const [previewViewMode, setPreviewViewMode] = useState<PreviewViewMode>("source");

// 派生视图 — 现有 PanelZone / FileTreePanel 等继续读 previewFile 零改动
const previewFile: string | null =
  previewSource?.kind === 'file' ? previewSource.filePath : null;

// 主 setter：同时联动 previewOpen + previewViewMode（inline 默认 rendered）
const setPreviewSource = useCallback((source: PreviewSource | null) => {
  setPreviewSourceRaw(source);
  if (source) {
    // file 沿用原 defaultViewMode；inline-html/inline-jsx 强制 rendered
    //（inline-datatable 的 viewMode 不重要，但保持 rendered 以便未来扩展）
    if (source.kind === 'file') {
      setPreviewViewMode(defaultViewMode(source.filePath));
    } else {
      setPreviewViewMode('rendered');
    }
    setPreviewOpen(true);
  } else {
    setPreviewOpen(false);
  }
}, []);

// 兼容 adapter：老调用 setPreviewFile(path) 内部转成 setPreviewSource
const setPreviewFile = useCallback((path: string | null) => {
  if (path === null) {
    setPreviewSource(null);
  } else {
    setPreviewSource({ kind: 'file', filePath: path });
  }
}, [setPreviewSource]);

// 路由切换 effect 保持原行为（src/components/layout/AppShell.tsx:370-373）
useEffect(() => {
  setPreviewSourceRaw(null);
  setPreviewOpen(false);
}, [pathname]);
```

并在 `useMemo` 返回值里同时导出 `previewSource` / `setPreviewSource` / `previewFile` / `setPreviewFile`，依赖数组加 `previewSource`、`setPreviewSource`。

### 约束检查（让 TypeScript 帮忙）

- 所有 `PreviewSource` 分支都带 `kind` 字面量，调用 `switch(source.kind)` 时 exhaustiveness 可被 TS 强制（建议在 `PreviewPanel` 的渲染 switch 里配 `const _exhaustive: never = source;` 兜底分支）
- `setPreviewFile` 参数仍是 `string | null`，外部零改动
- `previewFile` 读取端拿到的仍是 `string | null`；不能把 inline-* 漏判成有路径

---

## [迁移清单]

| 文件 | 行号 | 当前用法 | 迁移方式 | 风险 |
|---|---|---|---|---|
| `src/hooks/usePanel.ts` | L47 | `previewFile: string \| null` | **直接扩展** — 新增 `previewSource: PreviewSource \| null`，保留 `previewFile` 做 adapter 字段 | 低。类型新增不破坏既有 consumer。需同时 `export type PreviewSource` |
| `src/hooks/usePanel.ts` | L48 | `setPreviewFile: (path: string \| null) => void` | **保留兼容 adapter** — 新增 `setPreviewSource`，`setPreviewFile` 内部转 `setPreviewSource({ kind:'file', filePath })` | 低。调用方看到的签名不变 |
| `src/components/layout/AppShell.tsx` | L355 | `useState<string \| null>(null)` for `previewFile` | **直接改** — 改成 `useState<PreviewSource \| null>(null)`，重命名为 `previewSource` | 中。需确保路由 effect（L370-373）同步改为清 `previewSource` |
| `src/components/layout/AppShell.tsx` | L358-366 | `setPreviewFile` 带副作用（设 viewMode + open） | **改为 `setPreviewSource`** — 副作用逻辑保留，`file` 分支走 `defaultViewMode(filePath)`，`inline-*` 默认 `rendered`；`setPreviewFile` 变薄 adapter | 中。新增的 inline-html 如果调用方希望 source 视图会失配 —— 但当前需求明确 inline 都是渲染形态，可接受 |
| `src/components/layout/AppShell.tsx` | L371 | `setPreviewFileRaw(null)` in pathname effect | **改** — `setPreviewSourceRaw(null)` | 低。语义一致 |
| `src/components/layout/AppShell.tsx` | L447-448 | `useMemo` 返回 `previewFile, setPreviewFile` | **同时暴露 4 个字段** — `previewSource, setPreviewSource, previewFile, setPreviewFile` | 低。新增字段不影响旧读者 |
| `src/components/layout/AppShell.tsx` | L452 | 依赖数组含 `previewFile, setPreviewFile` | **加 `previewSource, setPreviewSource`** 到依赖数组 | 低。React 依赖完整性检查 |
| `src/components/layout/PanelZone.tsx` | L13 | 解构 `previewFile` | **直接改** — 解构 `previewSource`（不再需要 `previewFile`） | 低。仅本文件 |
| `src/components/layout/PanelZone.tsx` | L15 | `anyOpen = (previewOpen && !!previewFile) \|\| ...` | **直接改** — `(previewOpen && !!previewSource) \|\| ...`（Phase 1.5 关键点） | **高** — 漏改则 inline-* 面板永远挂不上。必须在该行验证 |
| `src/components/layout/PanelZone.tsx` | L22 | `{previewOpen && previewFile && <PreviewPanel />}` | **直接改** — `{previewOpen && previewSource && <PreviewPanel />}` | **高** — 同上 |
| `src/components/layout/panels/PreviewPanel.tsx` | L90 | 解构 `previewFile, setPreviewFile` | **渐进改** — 加 `previewSource`，保留 `previewFile` 过渡；下一步把整个组件的渲染分支改为基于 `previewSource.kind` switch | 中。一次性全改需要同步改 L103-236 多处 |
| `src/components/layout/panels/PreviewPanel.tsx` | L103 | `const filePath = previewFile \|\| ""` | **重构** — 改为 `const filePath = previewSource?.kind === 'file' ? previewSource.filePath : ''`，或 switch 分派 | 中。所有下游（fileName/breadcrumb/canRender/isMedia/fileServeUrl）依赖此 |
| `src/components/layout/panels/PreviewPanel.tsx` | L105-142 | `useEffect(loadPreview)` 基于 `filePath` 发 API | **增加 guard** — `if (previewSource?.kind !== 'file') return;`，其他 kind 不发请求 | 中。需避免 inline-* 场景误触 `/api/files/preview` |
| `src/components/layout/panels/PreviewPanel.tsx` | L144-149 | `handleCopyContent`：`preview?.content \|\| filePath` | **扩展** — `inline-html` 复制 `source.html`，`inline-jsx` 复制 `source.jsx`，`inline-datatable` 复制序列化 TSV/JSON | 低。新逻辑分支，旧 file 路径不动 |
| `src/components/layout/panels/PreviewPanel.tsx` | L151-154 | `handleClose` 调 `setPreviewFile(null) + setPreviewOpen(false)` | **改** — 直接调 `setPreviewSource(null)`（它已联动 `setPreviewOpen(false)`） | 低。语义不变 |
| `src/components/layout/panels/PreviewPanel.tsx` | L156 | `fileName = filePath.split("/").pop()` | **扩展** — inline-* 回退到 `source.virtualName ?? <kind 默认名>`（如 `'preview.html'` / `'preview.jsx'` / `'table'`） | 中。header 标题展示要合理 |
| `src/components/layout/panels/PreviewPanel.tsx` | L158-163 | `breadcrumb` 基于 `filePath.split("/")` | **扩展** — inline-* 返回 `virtualName` 或空串（breadcrumb 可能整行隐藏） | 低 |
| `src/components/layout/panels/PreviewPanel.tsx` | L165-175 | `canRender`/`isMedia`/`fileServeUrl` 全部基于 `filePath` | **按 kind 分派** — inline-html 走 iframe；inline-jsx 走 Sandpack（Phase 2.1）；inline-datatable 走 DataTable viewer（Phase 5.4）；file 保持原逻辑 | **高** — Phase 1.5 需先预留空分支（inline-jsx/inline-datatable 暂返回 "coming soon" 占位），Phase 2/5.4 再填实现 |
| `src/components/layout/panels/PreviewPanel.tsx` | L186-238 | 渲染主分支按 `isMedia`/`canRender && rendered`/else 三叉 | **重构为按 `previewSource.kind` switch** — 更清晰，TS exhaustive | 中。大段重排，需要小心 loading/error 状态在 inline 分支的处理（inline 无 loading） |
| `src/components/layout/panels/PreviewPanel.tsx` | L222-234 | `MediaView` / `RenderedView` / `SourceView` 接收 `filePath` prop | **保留** — file 分支继续传 filePath；新加的 inline 渲染组件（`InlineHtmlView` / `InlineJsxView` / `InlineDataTableView`）直接接 source | 低。分支隔离 |
| `src/components/layout/panels/FileTreePanel.tsx` | L18 | 解构 `previewFile, setPreviewFile, setPreviewOpen` | **通过 adapter 兼容** — 保持不变 | 低。adapter 保证行为一致 |
| `src/components/layout/panels/FileTreePanel.tsx` | L48-55 | `handleFileSelect` toggle 用 `previewFile === path` 比对 | **通过 adapter 兼容** — 保持不变；adapter 派生的 `previewFile` 在 inline 状态下是 `null`，不会误匹配 | 中。如果用户在 inline-* 状态下点击文件树同一路径，`previewFile===path` 为 false → 走 else 分支调 `setPreviewFile(path)` → 切换到 file kind，符合预期 |

---

## [风险与注意事项]

### R1. PanelZone gate 漏改（最高风险）

执行计划 `:60` / `:376-378` 已明确标注。本 POC 再次强调：**两处（L15 + L22）必须同时改**，且 `anyOpen` 和条件挂载要一起改，避免某个分支漏 inline-* 导致行为不一致。建议 Phase 1.5 PR 里对这两行加 code comment 显式说明「必须用 previewSource 而非 previewFile」。

### R2. `PreviewPanel` 渲染分支大量基于扩展名判断，inline-* 没有扩展名

`canRender` / `isMedia` / `isHtml` 三个辅助（`src/components/layout/panels/PreviewPanel.tsx:59-82`）都是 `getExtension(filePath)` 查表，对 inline-* 无意义。Phase 1.5 要在顶层按 `previewSource.kind` 分派后再走各自专用渲染路径；**不要**试图给 inline 构造假的 `filePath = 'preview.html'` 复用老逻辑 — 会被 MediaView 之类误判。

### R3. `useEffect(loadPreview)` guard

`src/components/layout/panels/PreviewPanel.tsx:106` 已经跳过 `isMediaPreview(filePath)` 和空字符串；Phase 1.5 要**扩展 guard**：只有 `previewSource?.kind === 'file'` 才发请求。inline-* 场景如果漏 guard，会拿 `filePath = ''` 去打 API，可能收到 400（API 要求 path 非空），造成无害但嘈杂的错误日志。

### R4. `ViewModeToggle` 对 inline-* 的语义

`src/components/layout/panels/PreviewPanel.tsx:187-189` 的 `ViewModeToggle` 只在 `canRender && !isMedia` 时显示。`canRender` 对 inline-* 永远 false（因为基于扩展名）。

- `inline-html`: 建议强制 `rendered`，隐藏 toggle（iframe 预览为主；源码预览对普通用户价值低）
- `inline-jsx`: 同上，Sandpack 主导
- `inline-datatable`: 无 source/rendered 概念，toggle 不显示

Phase 1.5 实施时需重写 toggle 的显示条件，不能直接复用 `canRender && !isMedia`。

### R5. `FileTreePanel` toggle 语义与 adapter 的互动

`FileTreePanel.tsx:48-55` 比较 `previewFile === path` 决定是否「再次点击同文件关闭」。当面板正处于 inline-* 状态时，派生 `previewFile` 为 `null`，`previewFile === path` 必然 false，所以走 else 分支→切到 file kind。这是**正确行为**：用户在预览 Artifact 的 inline 状态下，点文件树某个文件，意图是看那个文件，不是再次 toggle 关闭。**不需要改 FileTreePanel**。

### R6. 路由切换的清理副作用

`AppShell.tsx:370-373` 的 `useEffect(..., [pathname])` 当前清 `previewFile`。迁移后要改为清 `previewSource`（一个 setter 就够）。**注意**：不要忘了同时清 `previewOpen`（原代码显式清了 `setPreviewOpen(false)`），这是防御性写法，防止两个状态失配。

### R7. `useMemo` 依赖数组

`AppShell.tsx:452` 加两个新字段 `previewSource, setPreviewSource` 到依赖数组。旧字段 `previewFile, setPreviewFile` 建议保留（它们现在是派生值，但保留可以让 React 内部比对更安全；ESLint 也会提示）。否则可能遇到 stale closure。

### R8. 序列化 / 持久化

目前 `previewFile` 不持久化（路由切换就清了）。`previewSource` 同策略：**不持久化**，刷新后恢复到 null 即可。inline-* 内容（尤其 `inline-html`/`inline-jsx`）可能很大，存 localStorage 有安全和尺寸风险。Phase 1.5 验收标准里不包含持久化。

### R9. `setPreviewSource(null)` 与旧 `setPreviewFile(null)` 等价性

老代码常连调 `setPreviewFile(null) + setPreviewOpen(false)`（例：`PreviewPanel.tsx:152-153`、`FileTreePanel.tsx:49-50`）。迁移后的 `setPreviewSource(null)` 已在 AppShell adapter 内部联动 `setPreviewOpen(false)`，因此调用点可简化为单调用；但**显式双调用不算错**，只是冗余。Phase 1.5 一并清理。

### R10. adapter 的单向性

`setPreviewFile(path)` → 通过 adapter 内部永远 produce `{ kind: 'file' }`。**反向不通**：无法通过 `setPreviewFile` 设置 inline-*。这是期望行为 — 旧 API 仅支持 file kind，新 API 必须直接用 `setPreviewSource`。文档和 JSDoc 要注明这点，避免后续接手人以为 `setPreviewFile` 已弃用可以删。

### R11. grep 覆盖度验证

本 POC grep 全仓确认的 `previewFile`/`setPreviewFile` 读写点仅 5 个文件（`usePanel.ts`、`AppShell.tsx`、`PanelZone.tsx`、`PreviewPanel.tsx`、`FileTreePanel.tsx`）；`previewOpen`/`setPreviewOpen` 同上加 AppShell 自身的 useState。`electron/` 和 `apps/` 目录无任何匹配。测试目录（`tests/`）不存在。Phase 1.5 实施前建议重跑一次 grep 作为 sanity check（计划变更后可能有新消费点）。

### R12. 验收 smoke test 建议

执行计划 `:389-390` 列了两个 smoke case：
- `setPreviewSource({ kind: 'inline-html', html: '<p>test</p>' })` → PanelZone 应挂上 PreviewPanel，iframe 渲染 `<p>test</p>`
- `setPreviewSource({ kind: 'inline-datatable', rows: [[1,2]], header: ['a','b'] })` → PanelZone 应挂上（即便 Phase 5.4 的 viewer 还没实现，至少面板框要能显示「coming soon」占位而不是被 gate 掉）

这两个 case 专门压 R1（gate 漏改）。建议写成 React Testing Library 单测而非 Playwright，成本更低。

---

## 附录：参考调用点（Phase 2 / 5.4 预埋，不在本 POC 范围）

- `docs/exec-plans/active/markdown-artifact-overhaul.md:442` — Artifact 卡片 `onPreview` → `setPreviewSource({ kind: 'file', filePath })`
- `docs/exec-plans/active/markdown-artifact-overhaul.md:858` — 表格 Artifact 点击 → `setPreviewSource({ kind: 'inline-datatable', rows, header })`
- `docs/exec-plans/active/markdown-artifact-overhaul.md:920` — 风险登记：PanelZone 漏点 smoke test

本 POC 仅调研 + 给出类型 + 迁移表，实际代码改动属于 Phase 1.5。
