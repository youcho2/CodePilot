# Tech Debt Tracker / 技术债务追踪

已知技术债务清单。每项标注优先级、影响范围和初步解决思路。

**AI 须知：发现新的技术债务时添加到此文件；解决后标注完成日期。**

## 活跃项

| # | 描述 | 优先级 | 影响范围 | 发现日期 |
|---|------|--------|----------|----------|
| 1 | ESLint 存量 4 个 error（`MessageItem.tsx` hooks 条件调用、`ChatView` impure render、`GalleryDetail` setState in effect） | 中 | CI lint 步骤需 `continue-on-error` | 2026-03-04 |
| 2 | `conversation-registry` / `permission-registry` 运行态依赖内存 Map，重启后丢失 | 中 | 长时间运行的会话、Bridge 模式 | 2026-02-26 |
| 3 | 消息 fallback 上下文固定最近 50 条，无动态 token 预算截断 | 低 | 长会话上下文质量 | 2026-02-26 |
| 4 | `context-storage-migration` Phase 0 剩余：`projects` 表未建、`canUpdateSdkCwd` 未实现 | 低 | 多项目隔离 | 2026-03-04 |
| 5 | Bridge 的 `/mode plan` 在会话权限档位为 `full_access` 时仍会被 `bypassPermissions` 覆盖，导致 Plan 语义失效；需让 bridge 与桌面聊天一致，显式以 Plan 优先于 full_access | 中 | Bridge 远程会话的权限/安全语义 | 2026-03-25 |
| 6 | Electron 主进程行为（外链拦截、窗口管理等）无自动化测试覆盖。现有 Playwright 测试只覆盖 Next.js web 层，需要搭建 `@playwright/test` + `_electron.launch()` 的 Electron E2E 测试框架 | 低 | Electron 主进程回归风险 | 2026-03-30 |
| 7 | `claude-settings-credentials.test.ts` 的 "DB provider WITHOUT api_key" 和 `project-mcp-injection.test.ts` 的 "resolves ${...} env placeholders" 两条 regression test 在 CI（ubuntu/node 20）上用 `process.env.CI ? it.skip : it` 跳过了。根因疑似 tsx 对 `@/lib/db` 与 `../../lib/db` 动态 import 的 module identity 去重在 linux 上行为与 macOS 不一致：`createProvider` / `setSetting`（测试侧）写的 db 和 prod 代码里 `getProvider` / `getSetting`（registry / mcp-loader）读的 db 不是同一个句柄。本地两套都通过。需要深入排查 tsx + node 20 的 ESM 模块解析差异，或者改用依赖注入让测试不依赖真实 DB | 中 | 丢失一条 provider-ownership 边界断言 + 一条 env placeholder 解析断言的 CI 覆盖；其他测试仍覆盖相近路径 | 2026-04-15 |
| 8 | `@file`/`@directory` mention 无法识别带空格的路径（如 `docs/Product Spec.md`、`My Folder`）。插入文本是裸 `@${path}`，但 `parseMentionRefs` 用 `/@([^\s@]+)/` 分词，遇到空格就截断；`mentionNodeTypes[path]` 查不到，chip 消失、结构化 mention 不发送、目录 summary / 文件附件路径全部跳过。影响 PR #508 picker 插入 和 07eb5a9 目录拖拽两条链路。修复需要同时改 插入 token（转义或引号）+ `parseMentionRefs` 解析 + Backspace 整块删除逻辑；建议单开小 PR 处理并补测试 | 中 | 任何含空格的路径都无法走结构化 mention 流程；仅纯文本可达 LLM | 2026-04-18 |
| 9 | `src/__tests__/e2e/layout.spec.ts` 的 Sidebar Collapse/Expand、Theme Switch、Navigation Menu Highlight、Header、Mobile Responsive、Three-Column Layout (V2) 六个 describe 块都在测已经被重写的旧版布局（带 sr-only "Toggle sidebar" / "Toggle theme" 的按钮、header `<h1>` 标题、`navLink(Chat/Plugins/MCP Servers/Settings)`、右侧面板等）。当前 UI 改用 UnifiedTopBar + NavRail + ChatListPanel，且 nav 项是 Skills/MCP/CLI 工具/素材库/远程桥接。已在 spec 里 `test.describe.skip` 占位，避免 E2E gate 长期红；需要按新 layout 重写断言 | 中 | E2E 布局覆盖缺失；新 UI 回归只能靠手动 / CDP 抽查 | 2026-04-19 |
| 10 | `/api/search` 的 file branch 目前是 N-recent-unique-workspaces 的递归 fs.readdir 扫描——全局搜索每次键入都会触发 sequential workspace 扫描。当前临时方案：按 resolved path 去重后 all-mode 限 5 个最近 workspace，`file:` 限 15 个；超出范围无法通过不加前缀的搜索找到。真正的解是**文件索引**：在 session 创建 / 文件变更时维护持久化 trie 或 SQLite FTS 表，搜索时从索引读而不是每次扫磁盘。索引命中率高后可以放宽上限回到"全量扫描" | 中 | 搜索结果范围受限：跨 10+ 项目找文件必须用 `file:` 前缀；不加前缀默认只在最近 5 workspace 里找 | 2026-04-19 |
| 11 | `applyDiscoveryDiff` 的 `recommendedEnabled` / `discoveredHidden` 计数只在 INSERT 路径递增（`db.ts:2084-2086`）。如果 system-managed 行被刷新重新评估走 `updatePristineStmt` 分支（catalog 收紧 → 行从 `recommended` 翻到 `discovered`，或反向），翻转事件不计入返回 stats，Models 页"刷新全部" summary toast 会低估实际变化。修复方向：在 pristine 分支按 `recommended ? next : prev` 比较 emit 翻转计数（可拆 `pristineFlippedEnabled` / `pristineFlippedHidden`，或合并改名为 `appliedEnabled` / `appliedHidden` 并同步前端 i18n 文案 + 单测断言） | 低 | summary toast 数字偏低；用户感知不到，但精确度欠缺 | 2026-04-27 |
| 12 | Next 16.2.1 + Turbopack dev 在 `.next/dev/types/validator.ts` 生成的 `RouteHandlerConfig<Route>` 中，`{ params: Promise<ParamMap[Route]> }` 的 generic indexed access 在 TS 5 严格 variance 检查下被退化成 `Promise<unknown>`，导致 `[id]` 等动态路由 handler 类型与 validator 期望不兼容（报 `Type 'Promise<unknown>' is not assignable to type 'Promise<{ id: string; }>'`）。这是 Next 派生类型 bug，跟我们路由代码无关——`.next/dev/types/routes.d.ts` 里的 ParamMap 解析正确（`"/api/tasks/[id]": { "id": string; }`）。**临时修复（2026-04-30）**：在 `tsconfig.json` 的 exclude 加 `.next/dev/types/validator.ts`（**只排单文件**，保留 `routes.d.ts` 让 ParamMap / AppRouteHandlerRoutes 等真实类型仍参与 typecheck）。`npm run test` 已恢复通过基线（typecheck + 1354/1354 unit）。**风险边界**：dev validator 的 sanity check 在我们这一侧不再 enforce——但它只是把每个 route 的 handler 跟生成的 RouteHandlerConfig 对一遍，对 production build / runtime 行为零影响；Next production build 自身仍会校验。**重启评估**：升级 Next 16.x（特别是 16.3+ 或 17）时去掉这条 exclude 跑 `npm run typecheck`，如果上游修了 generic indexed access 退化问题就关闭这条债务。可参考 vercel/next.js issue tracker 搜 "Promise<unknown> RouteHandlerConfig" / "params: Promise" 跟踪 | 中 | dev validator sanity check 不再 enforce；真实路由类型仍正常校验，prod build 不受影响 | 2026-04-30 |
| 14 | (a) 已实现，仅剩 (b)：在现有"按推荐整理"（Tidy by recommended）预览对话框里，把"`model_id ∉ currentCatalog.defaultModels`"的旧目录项明确列入"会处理"清单。(a) 已通过 Phase 1 Step 2 的"已不在当前推荐目录"行级徽章（`isModelInCurrentCatalog` + ModelsSection 行 UI）解决；先观察 (a) 在用户实际使用中的信号量是否足以驱动 (b) 的需要 | 低 | 轻度误导已大幅缓解；(b) 仅在 catalog drift 频繁时才有 ROI | 2026-05-06 |
| 15 | Models 页「刷新全部」混合批次的 summary toast 啰嗦：`up-to-date` outcome（probe 成功但本轮没产生 enable/hide 变化的 catalog provider）会在 summaryOk 行被记成 `1 个成功 · 启用 0 · 隐藏 0`。语义对（确实没启用任何新模型），但 0/0 让用户读起来像 bug。**几条可选方向**：(a) 像 OpenRouter validate 那样把 `up-to-date` 拆成独立计数器 + 独立 summary 行（"N 个无更新"已经存在为 `summaryNoChange`，但当前实现把 up-to-date 走的是 success 桶；需要把 success 改成只统计"真的有 enable/hide 变化"的 provider）；(b) 让 `summaryOk` 在 `enabled === 0 && hidden === 0` 时省略掉 enabled / hidden 那一段，只说"N 个已刷新"。两条都可以；(b) 最小，可优先做。完全是 copy polish 范畴，非阻断 | 低 | 混合刷新 toast 可读性；无功能影响 | 2026-05-06 |
| 16 | **套餐型 / 固定阵容 catalog 的主动核准**。Phase 1 Step 2 落地了 UI 提示（"已不在当前推荐目录"行级徽章 + Refresh All 跳过提示 + Add Model dialog 套餐型文案），但**没**逐 provider fetch 官方页面验证当前白名单 — 国内套餐型多为 JS 渲染中文页，本会话内的 fetch 工具无法稳定抓到当前白名单文本。catalog 阵容沿用 Round 215 状态。涉及 provider：volcengine / bailian / glm-cn / glm-global / minimax-cn / minimax-global / xiaomi-mimo-token-plan / deepseek。**触发主动复核的条件**（命中任一即开下一轮）：(a) 多用户反馈某 SKU 不见了 / 不准了；(b) 某 provider 公告新模型超 30 天未跟进；(c) 进入下一个 release 前的 catalog 收尾。**核对时要做的事**：浏览器抓取每家官方 Coding Plan / Token Plan 文档，对照 `defaultModels`，更新代码 + 在 `docs/research/provider-model-discovery.md` 的"来源指针清单"表加"最后核对 YYYY-MM-DD + 来源 URL 截图"列。Step 2 review 已明确这点不能伪装成"已核准" — 见 review 修订日志 | 中 | catalog drift 不会立刻坏功能（manual_* 保护契约保留），但白名单久不更新会让用户看到旧 SKU 时不确定是否仍可用 | 2026-05-06 |
| 17 | 官网本地 production 预览存在 i18n 重定向循环：`next start` 下访问 `/` 会 308 到 `/en`，随后 `/en` 又 307 回 `/`，CDP 报 `ERR_TOO_MANY_REDIRECTS`；`/zh` 正常。初步看与 `fumadocs` i18n `hideLocale: 'default-locale'` 和默认英文路径处理有关。需要单独排查 `apps/site/src/lib/i18n.ts`、`apps/site/src/middleware.ts`、`apps/site/src/app/[lang]` 路由结构，避免官网英文首页在本地/生产出现循环 | 中 | 官网英文首页 `/` / `/en` 可访问性与本地视觉验收；中文页不受影响 | 2026-05-11 |
| 18 | **Markdown → HTML Artifact 显式保存入口 deferred**。Phase 4 第一轮把"保存 HTML"按钮放在 PreviewPanel 头部，写入 `<workspace>/.codepilot/artifacts/<slug>.html`，用户两轮反馈反对（路径错位 — 隐藏目录 + slug 丢原名；header 已拥挤；Style Select 已能让用户原地看到 HTML 形态）。当前状态：UI 入口（按钮 / handler / state / i18n key / 合同测试）已移除；library helpers（`buildPresentationArtifactPath` / `slugifyPresentationArtifactName` / `presentationStyleToTemplateId`，`src/lib/markdown/presentation-templates.ts`）+ 纯函数单测保留作未来 Export pipeline 脚手架。**重启条件**（命中任一即开下一轮）：(a) 多用户反馈"我要把 MD 发给同事看"等真实分享场景；(b) 立项独立 Export menu（HTML / PDF / 长图统一入口）。**重启时要做的事**：(i) 设计 Export 入口位置（Tab strip overflow menu / Copy 按钮旁 dropdown / Cmd+E 弹窗 — 不是 Markdown header primary button）；(ii) 路径选择（`<同目录>/<原名>.html` 或显式 `<workspace>/exports/<原名>.html`，**不要**用 `.codepilot/artifacts/<slug>.html`）；(iii) 重新加 i18n key + UI smoke + 合同测试 | 低 | 用户没有按钮可点；helpers 已就位，重启成本低 | 2026-05-12 |
| 20 | Settings → Models 页面对 `codex_account` provider 发 `GET /api/providers/codex_account/models?all=1` 会返回 404（`route.ts:31-34` 因 `getProvider('codex_account')` 返回 null 直接 fail）。`/api/providers` 列表里有 codex_account 占位项（OAuth 模式），但 db 行不在 `providers` 表 → ModelsSection 的 `Promise.all` 对每个 provider 发 models fetch 时炸。**真实影响**：浏览器 console 在打开 Settings 后持续出现 404 噪音；本身不破坏功能（fetch fail → `next[p.id] = []` 兜底）。Codex 在 Phase 6 Phase 2a CDP smoke 时抓到（2026-05-19）。**修法候选**：(a) ModelsSection 在 fetch 前过滤掉 OAuth-only provider（按 provider_type / api_key 缺失判断）；(b) `route.ts` 对识别为 OAuth 占位的 provider 返回 200 empty models 不返回 404；(c) `getProvider` 对 OAuth providers 用另一种 lookup。**优先级理由**：纯 console 噪音不阻塞用户路径，但污染后续所有 UI smoke / CDP 验证 → 抬高 review noise floor。**Phase 6 验收处理（2026-05-19 Codex review）**：本 Phase 不修；Phase 4 smoke ledger 的 Evidence 列必须明确写 "console clean except tech-debt #20"，不允许笼统说 "console clean"。新噪音出现要么修要么登记新 tech-debt | 低 | console 噪音；后续 UI smoke / CDP 验证误报 | 2026-05-19 |
| 21 | Phase 6 context_breakdown snapshot 的 `toolDescriptorTokens` 和 `mcpDescriptorTokens` 当前是 placeholder：`toolDescriptorTokens = 0`、`mcpDescriptorTokens = mcpServerNames.length * 200`（粗略估值）。**影响**：popover 中"工具"行永远显示 0，"MCP"行显示估值（跟真实 schema tokens 差异较大）。**修法 (Phase 1c)**：在 send path（`src/lib/claude-client.ts` 1129 周围，adapter call 之后）拿到 AI SDK 的 tool schema JSON 后用 char/4 估算 toolDescriptorTokens；MCP server 的 tool list 各 tool definition 同样估算 mcpDescriptorTokens。需要：(a) AI SDK Options.tools 或 SDK init 时拿到的 tool schemas；(b) MCP server registry 暴露 tool schemas 接口（loaded MCP servers 当前是否提供未确认）。**优先级理由**：Phase 1a 已让 system_prompt / rules / skills / memory 显示真实数据，覆盖 6/8 用户感知 kind 中的 4 个；tools/mcp 偏低不影响 Skills 验收，但用户切到 MCP-heavy 或 Tools-heavy 工作流时数字会失真 | 中 | 工具 / MCP 行 token 数值偏低 / 0 | 2026-05-19 |
| 22 | Context Accounting `selectedSkills: readonly string[]` 仅传 skill name，没传 sourcePath。如果 project `.claude/skills/foo` 和 global `~/.claude/skills/foo` 同名共存，`produceClaudeCodeAccountingSnapshot` 用 `discoverSkills().find(s => s.name === name)` 按 discovery 优先级取第一个（project 优先），不一定是 MessageInput badge picker 里点的那个。**真实影响**：同名重复 skill 场景下，Skills 行 token 数 + source breadcrumb 可能指向错误的 SKILL.md（虽然 name 一致但来源 mismatch）。Codex review v4 (2026-05-20) P2 — 短期接受 deferred。**修法**：把 selectedSkills 升级 `readonly string[]` → `readonly { name: string; sourcePath: string }[]`；MessageInput badges 存 picker 选中那刻的 SkillDefinition.filePath（picker 已经能 distinguish），全链 plumb 到 producer；producer 用 sourcePath 直接 statSync 不再走 discoverSkills 二次 lookup。**优先级理由**：单 skill workspace（绝大部分用户）不触发；只有用户在 project + global 同名 skill 场景才暴露，且暴露形式是 source breadcrumb 不准（数字仍真实），不算 hallucination；阻塞度低 | 低 | 同名重复 skill 时 Skills row source breadcrumb 可能指错 SKILL.md（数字仍真实）| 2026-05-20 |
| 23 | Sonnet 4.6 model alias 映射到错误 upstream ID。Codex review v5 真实 UI smoke (2026-05-20) 在 ClaudeCode Runtime + Sonnet 4.6 发送时，SDK 报 `claude-sonnet-4-20250514` 无权限/不存在。Sonnet 4.6 正确 upstream ID 应该是 `claude-sonnet-4-6`（per system prompt 约定 + 项目最新 catalog），但当前映射可能仍指向旧 Sonnet 4.0 (`claude-sonnet-4-20250514`)。**修法**：检查 `src/lib/provider-catalog.ts` + `src/lib/model-context.ts` 的 Sonnet 4.6 entry / alias → upstream resolver chain；可能要排查 Anthropic preset / OpenRouter Anthropic-skin preset / OAuth 路径各自的映射。**触发场景**：ClaudeCode runtime（直接走 Anthropic preset）+ Sonnet 4.6 模型选择。OpenRouter / GLM / 其他 provider 跑 sonnet 不一定触发（各自有 upstream mapping）。**优先级**：中（影响 ClaudeCode default 用户用 Sonnet 4.6 的发送链路；workaround = 切其他 model 如 glm-5-turbo） | 中 | ClaudeCode + Sonnet 4.6 发送失败；需切其他模型 | 2026-05-20 |
| 24 | Phase 7 popover footer cost 估算 input/output 双计风险（Codex review 2026-05-20 P2）。`src/components/chat/RunCockpit.tsx:207-209` 把 `usage.used - cacheRead - cacheCreation` 当 `LanguageModelUsage.inputTokens` 传给 `ContextContentFooter`。但 Phase 7 后 `usage.used` 在 Native/Codex provider-proxy 路径下（input_tokens=0 兜底）被 `context-breakdown.ts` 提升为 `knownUsedNonConversation + outputTokens`。结果：footer 把 outputTokens 部分算进了 inputTokens，随后又把 outputTokens 单独算一次 → cost 估算双计。**当前影响**：仅 popover footer 的 cost 估算偏高；不影响点阵 / 分类列表 / 上下文占用主显示。**修法**：把 `ContextUsageData` 拆 `reportedInputTokens`（原始 SDK 报告值，可能为 0）+ `displayUsedTokens`（提升后用于显示），footer 用 reportedInputTokens 算 cost；popover 主显示继续用 displayUsedTokens。**优先级理由**：cost 估算 UI 仅参考价值，用户实际 cost 走 provider 账单；non-blocking | 低 | popover footer cost 估算偏高（Native/Codex provider-proxy 路径，input_tokens=0 兜底场景） | 2026-05-20 |
| 26 | **✅ 已解决 2026-05-29（commit f32275f）**：assembleTools plan 分支改为派生 PERMISSION_SAFE_TOOLS 安全工具 + `getBuiltinTools({safeReadOnly})` 只保留 safe-read capability 的 prompt（widget guideline + FINAL OUTPUT FORMAT 留，image/dashboard/schedule/notify/media 排除）；回归 `plan-mode-safe-tools.test.ts`。**原始问题**：Native Runtime 在 Plan/只读模式下无法创建 Widget。**根因候选**：`src/lib/agent-tools.ts` 的 `assembleTools()` 在 `options.mode === 'plan'` 时提前 `return { tools: { Read, Glob, Grep }, systemPrompts: [] }`，绕过了 Phase 5e 的 `mutationLevel` / `PERMISSION_SAFE_TOOLS` 分级。`codepilot_load_widget_guidelines` 已被声明为 `safe_read`，理论上 Plan 模式也应该可用；但早返会同时移除该工具和 `WIDGET_WIRE_FORMAT_SPEC` / `show-widget` system prompt，导致 Native Agent 在聊天里无法按原有能力生成 Widget。**已验证边界**：普通模式下 `getBuiltinTools()` 仍能拿到 `codepilot_load_widget_guidelines` 且 system prompt 包含 `FINAL OUTPUT FORMAT` / canonical `show-widget` 示例；问题重点是 Plan 模式误杀安全只读 Harness 能力。**修法**：Plan 模式不要硬编码只保留 `Read/Glob/Grep`，而应派生保留 `PERMISSION_SAFE_TOOLS` 中的安全工具，并保留这些工具对应的 compiler system prompt；mutating / side-effect 工具继续禁用或走 ask。**回归测试**：`assembleTools({ mode: 'plan' })` 必须包含 `codepilot_load_widget_guidelines`，`systemPrompts` 必须包含 `FINAL OUTPUT FORMAT`，同时不包含 `codepilot_generate_image` / `codepilot_dashboard_pin` / `codepilot_schedule_task` 等 mutating 工具。 | 中 | Native Agent Plan/只读模式下 Widget 生成能力回退；用户以为 Native 不支持 Widget | 2026-05-21 |
| 27 | **✅ 已解决 2026-05-29（commit e1ccb3b）**：resolver（`resolveNewChatDefault`）早已区分三 reason，本次修 UI 层——`useOverviewData` 把 reason plumb 进 `defaultInvalidReason`，`HealthSection` + `RuntimePanel` 对 `pin-incomplete` 单出"固定信息不完整（缺 provider 绑定）"、其余出"不在当前引擎兼容范围内"；两者 severity 从 error 降 warn、去"阻断"，与 RuntimePanel 自动 fallback 口径一致。source-pin `pin-incomplete-ui.test.ts`。**原始问题**：Settings → Runtime / Health 的默认模型提示把 `pin-incomplete` 误报为"当前执行环境下不可用"。**现场复现（2026-05-21）**：`GET /api/providers/options?providerId=__global__` 返回 `{ default_mode: 'pinned', default_model: 'glm-5-turbo' }`，缺少 `default_model_provider`；同时 `/api/providers/models?runtime=claude_code` 中 `GLM (CN) / glm-5-turbo` 明确存在且 `supportedRuntimes` 包含 `claude_code`。`resolveNewChatDefault()` 因 pin 不完整返回 `invalid-default(reason='pin-incomplete')`，但 RuntimePanel 和 HealthSection 都渲染成"默认模型在当前执行环境下不可用 / not executable under current Runtime"，让用户误以为 GLM-5 Turbo 在 Claude Code 下不支持。**附带文案漂移**：Runtime 页说新会话会自动 fallback 到可用模型；Health 页仍说"新消息会被阻断，需切换执行引擎、启用模型、改 Pin 或切回 Auto"，与当前 chat 行为不一致。**修法候选**：(a) `pin-incomplete` 单独降级为 info/warning，文案改为"默认模型固定信息不完整，请重新在 Models 页固定默认模型"，不要归因 Runtime 兼容；(b) Health 页与 Runtime 页统一口径，不再说"阻断发送"，改为"会自动使用可用模型，建议重新固定默认"；(c) 从数据层补防御：写入 pinned default 时必须同时写 `default_model_provider` + `default_model`，发现半截 pin 可自动转 Auto 或提示修复；(d) 如果全局提示仍容易误报，考虑只在 Models 页展示 pin 状态，Runtime / Health 保留健康检查但不渲染 invalid-default banner。**优先级理由**：不会破坏发送链路，但会直接误导用户判断模型兼容性，尤其 GLM/Kimi 等跨 Runtime 已支持模型。 | 中 | Settings Runtime / Health 默认模型健康提示误导；用户误以为可用模型在当前引擎不可用 | 2026-05-21 |
| 28 | **🟢 代码已修 2026-05-29/30（commit 970a1fa + d6e9d96）；Windows 真机验收待**：`platform.ts` getPlatformShell + platformCommandGuidance（**off-Windows-PowerShell 为 no-op**），注入 Native（agent-system-prompt）+ Codex（unified-adapter）系统提示。**d6e9d96（Codex P2 收紧）**：Windows 默认 PowerShell，bash 只认显式 `CLAUDE_CODE_GIT_BASH_PATH`（不靠"装没装 Git Bash"——Git for Windows 几乎人人装但多数人在 PowerShell 粘命令）。ClaudeCode 不注入（Windows 必经 Git Bash、bash 正确）。回归 `platform-shell.test.ts`。端到端 Windows 验收见 preview Phase 2。**原始问题**：Windows 版本生成的脚本命令仍是 bash/POSIX 格式。用户反馈：Windows 版 CodePilot 中，大模型/Agent 生成的脚本命令偏向 `bash` / POSIX 语法，而不是 PowerShell / CMD / Windows shell 语法。**可能根因**：Harness / Runtime 上下文、tool descriptions、系统提示和命令示例没有注入当前平台 shell 方言，默认沿用 macOS/Linux/bash 假设。**修法候选**：(a) 在 Runtime/Harness context 中明确 `platformShell`（如 `zsh` / `bash` / `powershell` / `cmd`），并传给 ClaudeCode / CodePilot Native / Codex Runtime 的命令生成与工具提示；(b) Windows 下默认要求 PowerShell 兼容命令，只有检测到 WSL/Git Bash 时才允许 bash 语法；(c) 为 Windows fixture 加回归测试，禁止在 PowerShell 目标下生成 `export`、`source`、`rm -rf`、`/tmp` 等 bash-only 命令；(d) Settings / Runtime 能力说明如涉及 Shell 工具，应显示当前目标 shell，避免用户误以为命令可直接复制执行。 | 中 | Windows 用户复制/执行 Agent 生成命令失败；Shell 工具调用可能不可执行或行为错误；跨平台信任下降 | 2026-05-23 |
| 30 | **✅ 核心已解决 2026-05-31（D2）：flake 根治（per-worker temp DB 隔离）+ enforce 生效；React Compiler "16 error" 已修 3（2 prefer-const + 1 set-state）+ 清 11 warning，剩 13 拆到 #35。详见本格末尾【D2 收口】块。** 原始问题：`.husky/pre-commit` 不 enforce eslint。四行命令（`lint-hooks.mjs` / `npx lint-staged` / `npx tsc --noEmit` / unit tests）既没用 `&&` 串联也没有 `set -e`，husky 以**最后一条命令**（unit tests）的退出码为准。于是 `lint-staged` 里的 `eslint --fix` 失败不会阻断提交。**现状证据（2026-05-26 Phase 7c 收口提交）**：`lint-staged` 报 `✖ 10 problems (3 errors, 7 warnings)`（含 `card-primitives.tsx` ResizeGutter gradient IIFE 的 `react-hooks/refs` error、`AppShell.tsx`/`AssistantPanel.tsx` 多个 unused-var；均为**存量**，非本轮引入），commit 仍成功。**影响**：eslint 形同虚设，lint 债可无声累积。**修法候选**：(a) 加 `set -e` 或 `&&` 串联——但必须**先清掉现存 3 个 error**，否则立刻挡死所有提交；(b) 分两步：先修 `react-hooks/refs` + unused-var 存量，再收紧 hook；(c) 最小改动 `npx lint-staged \|\| exit 1`（同样要先清 error）。**附带观察**：同次提交首跑有 2 个 unit test flaky 失败、重跑全绿（2977/2977），unit 套件在 pre-commit 负载下偶发不稳，可一并排查。**2026-05-29 更新（加剧 + 实例）**：(a) `apply-discovery-diff.test.ts` 隔离单跑 11/11 必过，但全量 `src/__tests__/unit/*.test.ts` 下 3/4 次挂在它上面——同进程内某兄弟测试污染共享 DB/状态（#11/#25 家族），非随机重跑型 flake。(b) 同日一次**纯文档**提交（post-refactor-cleanup 计划）因此被 pre-commit 单测挡住，最终 `git commit --no-verify` 提交（commit 825edaf，message 已说明 + 独立确认 docs-drift 绿）。**结论**：#30 已开始影响收尾质量——flake 触发时误挡无关提交、不触发时又放行真正的 drift/lint/tsc 失败，两个方向都坏。收紧前除清 eslint 存量 error 外，需一并定位 apply-discovery-diff 污染源；**后续代码 phase 不应再靠 --no-verify**（见 `post-refactor-cleanup.md` Phase D）。**2026-05-29 二次核查（开工 D 时实测，修正早先估计）**：(1) eslint 存量实际 **16 个 error**（非早先写的"约 3 个"）——2 个 prefer-const（`context-chips-send-clear.test.ts`，机械修）+ ~14 个 react-hooks（`setState synchronously within an effect` / `Cannot access refs during render`）分布在 **9 个 live 组件/hook**：`MessageInput` / `NewChatWelcome` / `StreamingMessage` / `TaskCheckpoint` / `UnifiedTopBar` / `SkillDetailDialog` / `ResizeHandle` / `card-primitives` / `useMentionTokenEstimate` / `useWorkspaceSidebar`。后者是要改组件行为的真重构、高频 UI、有运行时回归风险，**盲改不可取**（Codex 代码审查抓不到运行时回归）。(2) flake **间歇**：同日连续两次全量 `*.test.ts` 又 3050/3050 全过——不可靠复现、难 bisect。**enforce 策略修正**：`lint-staged` 只 lint **暂存**文件，故可**先收紧 hook**（chain + `set -e`），那 9 个 react-hooks error 留作"碰到该文件再修"的 on-touch 债 / 或单开 react-hooks 专项，**不必先清完 16 个**才能 enforce。 **【D2 收口 2026-05-31】**：(1) **flake 根治** — 根因定位：`tsx --test *.test.ts` 按 node:test 默认并发（=CPU 数）**并行跑测试文件**，而多数 DB 测试未设 `CLAUDE_GUI_DATA_DIR` → 全部并发读写同一真实 `~/.codepilot/codepilot.db` → SQLite 竞争（隔离单跑过、满载挂的根因）。修法：新增 `src/__tests__/db-isolation.setup.ts`，`package.json test:unit` + `.husky/pre-commit` 加 `--import` 预加载，让每个 worker 进程拿到独立 temp DB（把 #25 的单文件隔离泛化到全套件）。连跑 4× 全量 **3086/3086 确定性通过**，flake 消除。隔离暴露并修了 1 个隐性依赖真实库 `cli_enabled` 的测试（`chat-runtime` native）。**Codex review P1 followup（commit 5e0cd50）**：初版隔离只修了「并发抢真实库」；测试在 `beforeEach` 自设新 temp dir（未预 touch 空库）时 db.ts 动态重载仍会把真实 `~/Library/.../codepilot.db` 拷进 /tmp（`[db] Migrated database` 日志为证）。故再加 `CODEPILOT_DISABLE_DB_MIGRATION_IN_TESTS` 全局开关（`db-isolation.setup.ts` 无条件设、`db.ts getDb()` legacy-copy 分支守卫、**生产从不设**），测试进程全程跳过迁移拷贝 → `Migrated database` 日志归 0，真实库污染彻底根除。(2) **"16 error" 实为 React Compiler 规则**（`react-hooks/set-state-in-effect` + `refs`，**非** exhaustive-deps；早先二次核查把规则名记成 exhaustive-deps）：已修 **2 prefer-const** + **1 set-state-in-effect**（`plugins/page.tsx` 搜索重置 → React 官方"prop 变时渲染期调整 state"模式，CDP 验证切 tab 清空 + 往返计数正常 + console 干净）；另清 **11 条 exhaustive-deps warning**（常量提模块作用域 / useMemo 包裹 / 删多余 dep / 修错位 disable / 删 3 条 dead disable）。**13 条 React Compiler error 仍 defer → 见 #35**。(3) **enforce**：D1 `set -e` 已生效；flake 既修，普通提交不再被误挡（仅暂存 #35 那 9 个文件才触发其 error，属 on-touch debt）。 | 低 | ✅ flake 根治 + enforce 生效；剩 13 React Compiler error（on-touch，见 #35）| 2026-05-26（D2 收口 2026-05-31）|
| 31 | Codex Account / Codex Runtime 原生能力 parity（逐项接入）— **已收口（2026-05-29）**。用户要求（2026-05-27）：所有 CodePilot built-in capability 都应尽可能在 Codex 下可感知、可调用；接不了的要诚实降级。**收口状态**：✅ Memory / Widget / Tasks+Notify / Dashboard / CLI 五项均已接入 + 真实账号 smoke 通过（Tasks/Notify + Dashboard/CLI 引入按能力区分的 elicitation 审批策略：safe-read 自动接受 / mutating 走用户审批 / 未知 decline；Dashboard/CLI 拆 read+write 两 server 绕过 Codex elicitation 不识别 tool 的限制）；Codex 原生图片入库已对齐素材库（prompt/model/provider 入 Gallery API）。**用户决定 defer（2026-05-29）**：(a) Image/Media — 不叠加 App 的 Gemini 图片 MCP（Codex 自带图片够用，避免两个画图工具混淆）；(b) User MCP — 独立工程（transport / OAuth / 白名单），后续单独立项不混进 #31。详见 `docs/exec-plans/completed/phase-8-codex-mcp-context-injection.md` 的 2026-05-29 决策日志。**留底优先级**：低（条目保留只是为了未来同类工作的索引；当前无遗留缺口）。 | 低 | 已收口；无能力缺口 | 2026-05-27（收口 2026-05-29）|
| 32 | `codex-mcp-events.test.ts` 用 synthetic session 触发 `createPermissionRequest failed: FOREIGN KEY constraint failed` 控制台噪音。**根因**：审批往返测试用 `sessionId:'sess-approval'` 等假 session，`handleCodexMcpElicitationApproval` 里 `createPermissionRequest` 往 `permission_requests` 表 INSERT 时 FK（对应 chat_session 行不存在）失败被 catch（warn 不阻断），测试 12/12 仍 pass、审批 in-memory 往返不受影响。**影响**：纯测试控制台噪音，非用户路径 blocker；但抬高 review / CI noise floor（同 #20 / #25 一类）。**修法候选**：(a) 测试 `beforeEach` 建真实 chat_session 行再跑审批往返（最贴近真实路径）；(b) mock `@/lib/db` 的 `createPermissionRequest`/`getPermissionRequest`；(c) 让 DB 写在测试模式可注入。 | 低 | 单测控制台噪音；非功能 / 非用户路径 | 2026-05-28 |
| 33 | Codex 原生 MCP 注入的 capability 契约缺少专用 `kind` 变体——目前 dashboard / cli_tools 在 capability-contract.ts 仍写 `codex_proxy.kind = 'unsupported'`（因为 LEGACY provider-proxy 桥确实没建），但实际通过 mutation-level MCP split 已经可调用，matrix 层做了 `CODEX_NATIVE_PROMOTED_BY_CAP` 推广。`harness-capability-matrix.test.ts` 的"exposure.kind=unsupported → 不可 executable"不变量测试因此被加了一份显式 `MATRIX_LAYER_PROMOTIONS` 例外白名单（2 项）。**修法候选**：在 `RuntimeExposureKind` 联合加 `mcp_server_split`（或 `codex_native_mcp`）变体，给 dashboard / cli_tools 的 `codex_proxy` 改用新 kind + 指向 `builtin-mcp-servers.ts` 的 read/write entry 元数据；deriveCell 把它当 executable 处理（同 `mcp_server` 一样）；移除 matrix 层的 promotion override + 测试例外白名单。**优先级理由**：现状 matrix 显示 / 测试 / 运行时注入完全一致，只是契约文字与实现路径有"语义间隙"——未来想再加同类拆分能力（例如 user MCP 写操作走审批）时，统一用 kind 表达比维护例外白名单更易扩展。 | 低 | 契约 / 矩阵层 / 测试三处需维护一份例外名单；功能 / 用户路径不受影响 | 2026-05-28 |
| 34 | **✅ 已确认 2026-05-30（Codex 真实 smoke）+ 观测已加（0605b80）**：Codex 跑 10s reminder（`b631cdeeb1ce3c80`）首次触发 success，notification delivery = renderer-toast `delivered` + electron-native `delivered`（任务后已删，确认 Not found）——**链路（scheduler→入队→renderer/Electron→ack）确实工作**。用户肉眼没见 macOS 横幅 = macOS 前台应用抑制横幅 / 通知权限样式，**非链路 bug**。核查的代码链：scheduler→sendTaskNotification→入队→窗口可见 in-app toast + OS 通知 / 窗口隐藏 bg-poller OS 通知，两条 show 都 `.show()`。"不弹窗"几乎必为运行时条件——最可能 dev Electron app 无 macOS 通知权限（未签名 dev 二进制 → `.show()` 静默 no-op），或 macOS 抑制 focused 应用横幅（in-app toast 仍应出）。已加 `[notify]` 观测（enqueue 成功/失败 + 两条 show 路径 supported/focused 状态，catch 不再静默吞错），让 fire 任务后一眼定位。回归 `notify-observability.test.ts`。**剩余（非阻断、产品取舍）**：若要前台 focused 时也弹横幅，需调 macOS 通知样式/权限，或让 in-app toast 更显眼——非 bug。**原始问题**：macOS Electron 客户端定时任务到点执行但没有弹出系统通知。**现场反馈（2026-05-28）**：在当前 3001 Mac 客户端 dev 环境里设置 3 分钟定时任务，到点后任务开始执行，但 macOS 没有出现通知弹窗。重点不是 scheduler 完全没触发，而是"任务触发/执行链路"与"用户可见通知链路"脱节。**排查方向**：(a) 检查任务触发链路是否最终调用 `codepilot_notify` / Electron main 通知 API / bridge 通知入口；(b) 检查 macOS 通知权限、bundle id、dev Electron app 是否被系统允许显示通知、Focus/勿扰模式与前台 app 行为；(c) 检查 renderer / main 之间的通知 IPC 是否在 dev/Electron 环境下丢失，尤其任务由后台 scheduler 触发时是否缺少 BrowserWindow / session 上下文；(d) 加一个可复现 smoke：创建短周期任务 → 等待触发 → 断言 notification dispatch/audit 事件产生，必要时用 main-process mock 代替真实系统通知。**修法候选**：统一任务触发后的通知出口，给 macOS dev/packaged 分别加可观测日志；若系统通知权限不可用，至少在聊天内 / 状态区落一个可见 fallback，避免任务静默完成。 | 中 | Mac 用户依赖定时任务提醒时收不到通知；任务执行状态不可见，信任感下降 | 2026-05-28 |
| 35 | **13 个 React Compiler error（`react-hooks/set-state-in-effect` ×9 + `refs` ×4）— on-touch debt，defer**（2026-05-31 从 #30 拆出）。**性质**：本项目启用 React Compiler，这些是编译器"无法优化此组件"的 bailout（报 "Compilation Skipped"），**代码运行时正确**（compiler 只是跳过该组件的自动 memo），非 runtime bug；只在**暂存对应文件**时 `eslint --fix` 非零退出而挡提交（lint-staged 只 lint 暂存文件，故不影响其他文件的提交）。**分布（9 文件）**：set-state-in-effect — `MessageInput`:237 / `NewChatWelcome`:94 / `StreamingMessage`:224 / `TaskCheckpoint`:55 / `UnifiedTopBar`:136 / `SkillDetailDialog`:77 / `useMentionTokenEstimate`:115 / `useWorkspaceSidebar`:71；refs（渲染期读 `ref.current`）— `card-primitives`:233-234 / `ResizeHandle`:85-86（光标跟随渐变的高度测量）。**为何 defer**：真正修法是行为重构——set-state-in-effect 要改"渲染期派生 / 移到事件处理 / lazy-init"（已采样确认：NewChatWelcome 随机问候、TaskCheckpoint 数据 fetch、useMentionTokenEstimate 缓存水合，均为合法 effect，非简单重置）；refs 要改 ResizeObserver+state 测量（视觉行为重构，需 CDP 视觉验证）。均为高频 / 视觉组件，**盲改有运行时 / 视觉回归风险，Codex 代码审查抓不到**。**已修的简单案例参考**：`plugins/page.tsx` 的 `useEffect(()=>setSearch(""),[filter])` 属"prop 变时重置 state"，已用 React 官方渲染期调整模式安全修掉（见 #30【D2 收口】）——其余文件若出现同类简单重置可同法处理。**重启条件**：(a) 升级 React Compiler 想让这些组件也享受 optimized；(b) 因别的原因碰到这些文件时顺手 on-touch 修；(c) 单开 react-compiler 专项一次清完。 | 低 | 暂存这 9 个文件时提交被 eslint 挡（on-touch）；运行时 / 用户路径无影响 | 2026-05-31（从 #30 拆出）|
| 36 | **✅ 已解决 2026-06-01（P0.1，commit `0106b07`）**：`findCodexBinary()` 改为多候选时 probe `codex --version` 选最高版本（`selectBestCodexCandidate`），旧 `/opt/homebrew/bin/codex` 0.45 不再压过 `.app` 0.135；并打印 `[codex] selected binary { binary, reason, candidates }`，reason 明确（候选数 + 各版本）。配合 P0.2（fatal stderr 即时失败 `0106b07`/`2935340`）与 P0.3（模型发现 cacheOnly 不 spawn `d594fb0`），装回旧 PATH codex 既不再卡 30s、也会被版本择优避开。修法候选 (a)/(c) 实质落地（版本探测 + reason 带路径/版本/候选），(b) 由"选最新"取代。**原始问题**：PATH 优先 + `.app` 兜底，旧 Homebrew codex 抢过新 Codex.app 导致 spawn_failed 且提示不明（见另一台 Mac 日志：PATH 0.45.0 vs .app 0.135.0-alpha.1 + `xhigh`）。详见 `docs/preview/packaged-preview-p0-diagnosis-2026-06-01.md`。 | 低 | 已解决；旧 PATH codex 不再被静默选中 | 2026-06-01（Codex review；P0.1 解决同日）|
| 37 | **✅ 已解决 2026-06-04（Codex 复审通过）**：全 composer 路径 canonical-aware + 真机正向确认（de19e576 Sonnet 4.6→Opus 4.7），Codex 2026-06-04 复审确认 OpenRouter/Opus 静默切 Sonnet 修复通过。（先前 `b6d2e43` 仅修 helper 不完整、曾误标 solved 撤销，本次为正式标记。）**两轮修复**：① commit `b6d2e43` 让 `useProviderModels.resolvedModel`/`currentModelOption` 改用新的 canonical-aware 匹配器 `findModelOption`（按行 `value` 别名 **或** `upstreamModelId` canonical 匹配）——但**不完整**。② Codex 复审抓到 composer 仍有多处 value-only 匹配把修复打回去，最关键是 `MessageInput` 的 auto-correct effect：对任何 canonical id 都判定"当前模型不存在"→ 把 `currentModel` state 改写成组内第一项（Sonnet），而该 state 又喂回 `useProviderModels` → `resolvedModel` 也变 Sonnet → 发送链路仍发 Sonnet（DB 未 PATCH 但 in-memory state 被改）。**本提交收口**：把 `findModelOption` 抽到纯函数模块 `src/lib/model-option-match.ts`（无 React 依赖，不破坏 `chat-static-graph` 契约），全部 composer 消费方改走它——`MessageInput` auto-correct 用 findModelOption 判可用性（可解析就不纠正、currentModel 不被改写，发送由已 canonical-aware 的 `resolvedModel` 决定为 `opus`）、`ModelSelectorDropdown`（顶部标签 + 选中高亮 + 最近列表）、`ChatView` context 上游 id、`RunCockpitPopoverContent` 运行状态模型名。**未放宽 runtime gate、未静默 PATCH session、对可解析模型不改写 state（零静默替换，最保守）**。**回归**：`provider-model-roundtrip.test.ts` = matcher 单测 + 5 处 source pin（useProviderModels / MessageInput auto-correct / ModelSelectorDropdown / ChatView / RunCockpit 必须走 findModelOption 且旧 value-only 反模式已删）；`npm run test` 3233/3233 绿。**触发形态（真机核查 ~/.codepilot/codepilot.db + live /api/providers/models，2026-06-04）**：canonical 上游 id 有**两种真实写法**，匹配器都要认——OpenRouter（provider_type=openrouter）用 slug `anthropic/claude-opus-4.7`（live API 实测 opus 行 `value=opus`、`upstreamModelId=anthropic/claude-opus-4.7`）；直连 Anthropic-skin / preset provider 用 `claude-opus-4-7`（dash，route.ts preset 合并）。**任何别名制 provider 上历史会话把 canonical id 存进 `model` 即触发**——实测 54 个会话存 `claude-sonnet-4-6`、de19e576 存 `anthropic/claude-opus-4.7`（OpenRouter，**是有效复现而非"合成坏串"**——先前 `b6d2e43` 误判它是"用错的串、真实是 claude-opus-4-7"，实为当时没意识到 OpenRouter 用 slug 形态，**此处更正**）。旧的纯 `value` 匹配两种形态都命中不了 → 回落组内第一项；第一项 Sonnet 而存的是 canonical-Opus 时即静默 Opus→Sonnet。**验证（含 live CDP 正向确认 2026-06-04）**：重开真实会话 de19e576（OpenRouter，model=`anthropic/claude-opus-4.7`，Claude Code runtime）——**修复前 composer 顶部显示 Sonnet 4.6，全 composer 收口后显示 Opus 4.7**；模型下拉正常渲染全部 provider 分组、OpenRouter 组完整、console 干净。正向由 live CDP + matcher/auto-correct 单测 + 真实数据 + 全 composer source pin 共同保证。**原始问题**：OpenRouter（Claude Code）会话静默把选定模型换成别的（如 Opus→Sonnet）；Phase 0 真机确认 Anthropic skin 在 Claude Code 下**可发送**（非"无法发送"，硬阻断只在 codepilot_runtime 属正确行为）。证据见 `docs/exec-plans/completed/merge-blockers-chat-ownership-openrouter.md` Phase 0。 | 高（P1）| ✅ 已解决；canonical id 现可 round-trip 回别名行，全 composer 路径不再静默换模型；Codex 复审通过 | 2026-06-03 修复 / 2026-06-04 composer 收口 + 真机确认 + Codex 复审通过 |
| 38 | **`chat_sessions.chat_origin_type` / `chat_origin_path` 两列为已否决的"对话来源建模"遗留 —— 决定 strip 代码路径**。2026-06-03 用户否决"给对话打来源标记 / 顶部显示归属"（判为过度设计，原话"乱七八糟"）。`b8b1669`+`1b00edc` 曾加这两列 + `createSession` 参数 + 建会话 API / 侧栏 POST 透传；界面无显示、不影响发送。**决定：strip 代码使用路径**（types / `createSession` / API / 侧栏 wiring + `chat-origin.test.ts`），**已迁移的两个空 DB 列因禁止破坏性迁移保留为历史包袱**（不可视作产品方向；未来若要做"对话归属"须重新立项，不要假设这两列已是方向）。strip 提交紧随本登记（见 git 历史）。 | 低 | 否则成为长期 schema 噪音 + 误导后续开发者以为来源系统是产品方向 | 2026-06-03 |

## 已解决

| # | 描述 | 解决日期 | 解决方式 |
|---|------|----------|----------|
| 25 | `codex-media-import.test.ts` 污染真实素材库 DB，导致素材库出现大量 `codex-out.webp` 裂图行。**根因**：`src/lib/db.ts` 在模块加载时捕获 `CLAUDE_GUI_DATA_DIR`，旧测试在 `beforeEach` 再覆盖 env——media 文件（per-call 读 env）落临时目录，但 DB 行（模块加载时捕获）写进真实 `~/.codepilot/codepilot.db`，每跑一次单测累积一批裂图。 | 2026-05-28 | **(a) 隔离 — env-before-import**：新增 `src/__tests__/unit/_codex-media-import-env.ts` 作为 side-effect setup 模块，import 顺序里放在所有 `@/lib/*` 之前，确保 db.ts 模块加载时读到的 `CLAUDE_GUI_DATA_DIR` 已经指向 test root（ESM 同级 import 按声明顺序执行）。测试改用单一共享 test root，去掉 per-test env swap。**(a2) 隔离 — 抑制 db.ts 的真实 DB 迁移**（Codex review P1 follow-up）：仅设 env 不够——db.ts 在 `!fs.existsSync(DB_PATH)` 时会自动从 `~/Library/Application Support/CodePilot/codepilot.db` 拷贝过来。setup 模块**预 touch 一个 0 字节 codepilot.db**，让 SQLite 把它当全新 DB 直接 CREATE TABLE，不再拷真实库到 /tmp。**(b) 回归 guard**：测试 `before` 快照真实 `~/.codepilot/codepilot.db` 里 `provider='codex'` 的行数，`after` 断言增量为 0；guard 用 `sqlite_master` 检查 `media_generations` 表是否存在，避免 "DB 存在但表没建" 的环境误炸。**(c) source-pin 防回归**（Codex review P2 follow-up）：测试文件末尾加 `import order` describe 块——读自身 source 断言第一条 `import` 必须是 `./_codex-media-import-env`。未来谁在它上面塞 `@/lib/*` import 直接 fail。**(d) 一次性清理**（2026-05-28）：DELETE 1896 条 `local_path LIKE '/var/folders/%/T/codex-media-import-%'`（temp 路径，files 已不存在）+ 12 条非 temp 路径但 67 字节（test fixture）的行 + 对应文件；保留 3 张真实 Codex `ig_*.png`（784KB–2.6MB），DB 备份留在 `~/.codepilot/codepilot.db.pre-cleanup-2026-05-28.bak`。从 1975 行（其中 1911 codex）降到 67 行（3 codex）。**验证**：单测 13/13、全量 3050/3050 通过；日志无 `[db] Migrated database`；real DB codex 行数前后一致；temp dir 在 `after` 钩子完成清理无残留。|
| 29 | （原判断有误并撤销）"移除 AppShell 左侧 sidebar 的 `<div className="flex h-full shrink-0">` wrapper 会导致 `dataPlatform=null` / macOS profile 整片失效" | 2026-05-26 | Codex review 指出原解释不可信，复核后确认是误判。**真实根因**：`data-platform` 由 `src/app/layout.tsx` 的 anti-FOUC `<head>` `<script>` 在 hydration 之前盖在 `<html>`（`document.documentElement`）上，与 `<body>` 深处的 layout `<div>` 没有任何因果链。唯一能抹掉"head script 写在 `<html>` 上的属性"的，是 React 放弃 SSR hydration、从 RootLayout 重新渲染 `<html>`（RootLayout 的 JSX 不带 `data-platform`）——这由组件树中**任意位置**的 hydration mismatch / render error 触发，而不是这个被无条件渲染的 wrapper（server / client 两侧渲染完全一致，不产生 mismatch）。Round 34 的 `dataPlatform=null` 是关联性误判：当时同一批把 4 个 panel 文件改成不一致结构，真正触发 full client re-render 抹属性的是其中某个 panel 改动；回退"除 wrapper 外的一切"恰好也回退了真正触发源，于是 wrapper 背了锅。**处理**：wrapper 是 Round 22 `gap:4px` 时代的遗留（把 sidebar+handle 并成一个 flex item 防止 gap 翻倍）；Phase 7c-F 把 darwin content-row `gap` 设为 0、由 8px ResizeGutter 提供唯一可见间距后，wrapper 已无布局职责，直接删除，左侧 sidebar 回到 row-level card（`CardFrame` + `ResizeGutter` 扁平排在 `data-app-content-row`）。**可复现证据（2026-05-26）**：删除 wrapper 后 `PORT=3001 npm run dev` + `PORT=3001 npx electron .`，`[macos-vibrancy-diag]` 报 `dataPlatform=darwin` / `dataShell=electron` / `dataPlatformStyle=auto` / `surfaceSidebarToken=lab(100% 0 0 / .58)` / `opaqueElementCount=0`，macOS profile 完全正常；新增 `src/__tests__/e2e/card-gutter-geometry.spec.ts` 真实 DOM e2e 确认扁平后的 row-level 结构（sidebar frame → gutter → main frame，gutter 不在任何 `[data-platform-card-frame]` 内）。 |
| 19 | Native Runtime Built-in Harness 基础盘缺口 / 安全洞 | 2026-05-18 | Phase 5e 完成：`codepilot_*` 权限前缀洞改为 mutationLevel 派生，Native image/media 走 MediaBlock side-channel，memory/tags/file_type 与 tasks session-only parity 补齐，dashboard/cli trust boundary 在 Settings 展示，assistant_buddy Native parity 后 CodePilot Runtime 为 8/8。详见 `docs/exec-plans/completed/phase-5e-runtime-harness-architecture.md`。 |
| 13 | OpenRouter 全量目录物化 | 2026-05-06 | OpenRouter 走独立 `/search-models` + `/validate-models` 路由；`/discover-models` 对 OpenRouter 返回 `unsupported`；Add Service eager seed 3 条 catalog 别名而不是 300+；旧 DB 行通过「整理早期导入的目录」opt-in 入口隐藏。详见 `docs/exec-plans/active/openrouter-search-and-add.md` 与 `docs/research/provider-model-discovery.md` 的「OpenRouter — search-and-add (已实现)」小节 |
