# v0.56.x Stability / Trust 总计划

> 创建时间：2026-06-19
> 状态：🚧 Phase 0/1 已完成；Phase 2 进行中（Composer/Context 主链路 #1–#5 闭环；#629 已关闭 🟢，#635 代码层完成 🟡（待真实慢 proxy smoke），#632 假% 已修、item3 待续；stream 终态原因码 / 压缩失败回滚 / 诊断导出字段待续）；Phase 3 主问题 #628 已关闭 🟢（@file 落真实路径 + symlink 安全边界，Codex smoke 通过；余 Codex runtime 非图片 @file follow-up）；2026-06-28；Codex 负责审查、复现分析、测试设计与文档维护
> 来源：Notion 文档中心《CodePilot 项目审查：v0.56.x Stability 优先级》（2026-06-17，GPT Pro 分析）+ 当前 active plans / 本地工作区状态
> 版本口径：用户口头提到“5.6.x”，本文按仓库当前 `package.json` 版本统一写作 `v0.56.x`

## 一句话目标

v0.56.x 不追新能力，专门修“可信主路径”：对话不中断、上下文不串、文件不误改、预览不被多余确认阻断、安装更新可靠、出错可诊断、外部协作有质量门禁。

## 用户能看到什么

- Stop / 中断 / resume 后，同一个会话能继续发送，不再需要重启应用或新建会话。
- 长对话与上下文压缩失败时有明确原因和可诊断信息，不再默默串上下文或生成空 assistant 消息。
- 上下文用量不再显示假百分比或超过 100%；输入框和浮层只展示有真实来源的已用量与构成比例。
- 图片 / 文件等普通上下文附件不再触发“上下文太多，确认发送”这类阻断式横幅；Codex Runtime 下，图片 + 文本混合输入能按 Enter 正常发送，发送成功后输入框不会残留旧内容。
- 外部 Markdown / HTML / 本地网页预览直接打开，不再弹出多余“确定”确认；本地 HTML / 网页预览支持页面自身 JS 驱动的动态样式与交互，不只渲染静态 HTML。
- `@file` / 附件 / 可编辑源文件的语义清楚，AI 写操作不会误改 `.codepilot-uploads` 副本。
- Windows 安装、更新提示、日志导出与崩溃定位更可靠。
- GitHub issue / PR 有明确优先级、模板和 CI 门禁，外部贡献不再把主线拉散。

## 不做什么

- 不在 v0.56.x 主线新增 provider、bridge、Media Studio 能力、模块市场能力或大 UI 改版。
- 不把 Notion 文档提到的 issue 根因直接当事实；所有 GitHub issue 先重新核验当前状态、复现步骤和本地代码证据。
- 不把安全大项混入小修：API key 加密落盘、文件搜索索引、Runtime Capability Adapter 若进入 v0.56.x，必须单独拆 Phase / 执行计划。
- 不由 Codex 修改产品代码。产品 / 运行时 / Electron / 样式 / 构建脚本改动仍由 Claude Code 实施。

## 当前上下文与先后顺序

Claude Code 上一个进程已退出，当前工作区已有一批未提交改动。接手者必须先收束这些改动，再进入本总计划后续 Phase。

当前 `codebase-health-audit-2026-06.md` 的 Phase A 状态：

- A1 / A2 / A3 / A6 已标记完成，但产品代码与测试文件仍在工作区未提交。
- A4 `activeCodexTurns` 异常路径残留仍待核验。
- A5 权限超时静默拒绝仍待做，且 v2 已明确拆成“finalize helper”与“超时 UI 事件契约”两步。
- Smoke Ledger 中 A2 的 `ElapsedTimer` 动态视觉确认仍标为待确认。

**严格顺序：先 Phase 0，再进入 Phase 1+。**

> **2026-06-19 更新：Phase 0 已完成。** codebase-health Phase A（A1–A6）已收束并提交（`9c6e772` 主体 + `f99edbb` / `d00449f` 两个权限超时徽章 follow-up），`npm run test` 3360/3360 绿，工作区无残留 Phase A 脏改动（仅本 v0.56.x 文档 + README 索引行待提交）。A4（activeCodexTurns 移入 closeStream 单一出口）、A5（finalizePermission 单一出口 + 超时可见 SSE 契约 + 双入口徽章收口）均落地；A2 动态计时器确认为 sub-frame 瞬态、keyed-remount 构造性消除、截图不可判别，已在 codebase-health Smoke Ledger 记录。用户确认 Phase A 已通过 Codex 复审与 smoke。**现进入 Phase 1。**

## 状态总览

| Phase | 内容 | 状态 | 用户验收 |
|-------|------|------|----------|
| Phase 0 | 收完已开始的 codebase-health Phase A | ✅ 已完成（2026-06-19） | Phase A 改动完成、测试绿、未提交脏改动归零 |
| Phase 1 | v0.56.x 稳定性治理入口 | ✅ 已完成（2026-06-19） | GitHub milestone / labels / 对外冻结说明就绪 |
| Phase 2 | Session / Context / Stream / Composer 可信主链路 | 🚧 进行中（2026-06-20） | Stop / resume / context 压缩失败 / Codex 多模态发送均有一致状态与诊断 |
| Phase 3 | 文件引用与真实编辑路径安全 | ✅ 主问题关闭（#628 已修，Codex smoke 通过） | #628 @file 误改副本已修（in-tree mention 引真实路径 + cwd/symlink 校验）；余 follow-up：Codex runtime 非图片 @file 输入语义、`@file` 只读快照 vs 可编辑源语义分离 |
| Phase 4 | 安装 / 更新 / 日志 / 崩溃第一印象 | 📋 待开始 | Windows 安装、更新提示 CPU、Codex crash breadcrumb 收口 |
| Phase 5 | Electron / Preview / API / 权限安全硬化 | 📋 待开始 | API 不泄 stack，权限超时可见，外部 Markdown / HTML 预览无多余确认且 interactive sandbox 可用 |
| Phase 6 | 测试、CI 与 release blocker | 📋 待开始 | PR/main/nightly CI 分层，P0 未关不能发 stable |
| Phase 7 | GitHub Issues / PR 治理自动化 + 旧 issue 清库存 | 📋 待 Claude Code 一次性治理批次 | issue / PR 模板、标签、triage 规则、保守机器人与旧 issue 分桶收口 |
| Phase 8 | v0.56.x RC 与稳定版验收 | 📋 待开始 | v0.56.x RC smoke 通过，稳定版有明确 known issues / rollback |

## Phase 0 — 收完已开始的 codebase-health Phase A

**用户能看到什么**：长对话流式更顺、错误提示不再夹带堆栈、权限与 Codex 异常路径下一步有清晰收口。

**不做什么**：不跳去做新的 Notion 建议；不把未提交改动混入别的主题 commit。

**怎么验收**：

- `git status` 中已有 Phase A 改动被提交或明确拆分。
- `npm run test` 通过。
- A2 动态计时器视觉确认完成，或在 Smoke Ledger 明确记录暂未做与风险。
- A4 / A5 处理结果写回 `codebase-health-audit-2026-06.md`。

执行清单：

- [x] 先读 `docs/exec-plans/active/codebase-health-audit-2026-06.md` 最新状态与 Smoke Ledger。
- [x] 核对当前脏改动是否只属于 A1 / A2 / A3 / A6，不混入无关文件。
- [x] 对已完成项重跑 `npm run test`，并按需跑改动文件 lint / typecheck。（收束时 3360/3360 绿，见决策日志 2026-06-19。）
- [x] 处理 A2 `ElapsedTimer key={startedAt}` 的最小视觉确认：打开一个流式会话，确认新 turn 计时首帧不继承上一轮秒数。（按 keyed-remount 构造性消除 + 截图不可判别记录入 codebase-health Smoke Ledger，非逐帧视觉确认。）
- [x] 继续 A4：先读 `codex-stop-recovery.md`，核验 `activeCodexTurns` 在 turn 登记后、terminal event 前异常是否残留；若残留则修并加测试。（移入 closeStream 单一出口。）
- [x] 继续 A5 Step 1：抽 `finalizePermission` 单一出口，并加三路径 DB 持久化单测。
- [x] A5 Step 2 若要进入 UI：先写事件契约再改实现，两个聊天入口必须一致。（超时可见 SSE 契约 + 双入口徽章收口，follow-up `f99edbb`/`d00449f`。）
- [x] Phase A 收束后提交，commit body 必须包含根因 / 改动 / 验证 / 防回归。（`9c6e772` 主体。）

## Phase 1 — v0.56.x 稳定性治理入口

**用户能看到什么**：项目明确宣布 v0.56.x 是 Stability / Trust 线，外部用户知道新功能暂缓、P0/P1 优先。

**不做什么**：不借治理名义关闭真实 bug；不把未核验 issue 塞进 milestone。

**怎么验收**：

- GitHub 上存在 `v0.56.x Stability / Trust` milestone。
- P0/P1 label 体系与 issue 模板能指导用户提交可复现信息。
- README / Release Notes / pinned issue 中有一句清楚的冻结说明。

执行清单：

- [x] 用 GitHub 当前状态重新核验 Notion 提到的 `#626/#628/#629/#632/#633/#634/#635`：标题、状态、复现、是否仍适用本地代码。（两张源码复核表见下「2026-06-19 Codex 核验记录」+「Claude Code 源码复核」。）
- [x] 创建或更新 milestone：`v0.56.x Stability / Trust`。（GitHub milestone #1。）
- [x] 标签建议：（9 个标签已在 GitHub 建立。）
  - `P0-crash-or-interrupt`
  - `P0-data-loss-risk`
  - `P1-runtime-session`
  - `P1-context`
  - `P1-file-reference`
  - `P1-installer-update`
  - `P1-performance`
  - `needs-repro`
  - `v0.57+ parking-lot`
- [x] 对外说明草案：（已落地为 `.github/TRIAGE.md` + milestone #1 描述。）

```text
v0.56.x focuses on stability and trust: chat interruptions, context isolation,
session recovery, file reference safety, installer/update reliability, diagnostics,
and CI guardrails. New feature work is paused until these blockers are closed.
```

- [x] 更新 `docs/exec-plans/active/issue-tracker.md`，把已核验的新 issue 纳入统一看板。（〇节看板已纳入 7 issue。）

### 2026-06-19 Codex 核验记录

Notion 源页已拉取：`文档中心 / CodePilot 项目审查：v0.56.x Stability 优先级`（创建 / 更新：2026-06-17）。GitHub 通过 `gh` 实时查询：

- `#626/#628/#629/#632/#633/#634/#635` 均仍为 open；这些 issue 当前无 label、无 milestone、无 assignee。
- 仓库 open milestone 为空；labels 只有 GitHub 默认 9 个（bug / documentation / duplicate / enhancement / good first issue / help wanted / invalid / question / wontfix），没有 P0/P1、runtime-session、context、file-reference、installer-update 等治理标签。
- open PR 约 47 个；大量 PR 从 2026-02/03 起积压，绝大多数无 label，许多已 `CONFLICTING`；当前 PR 状态里 Vercel failure 噪音明显，不能替代项目自己的质量门禁。
- 最新稳定 release 为 `v0.56.0`（2026-06-10），资产包含 macOS dmg/zip、Windows `CodePilot.Setup.0.56.0.exe`、`SHA256SUMS.txt`；没有 Windows portable zip fallback。

Issue 逐条核验：

| Issue | 当前核验结论 | 本地证据 / 下一步 |
|-------|--------------|-------------------|
| `#635` 0.56 频繁自动中断 | 症状真实存在，但正文只有截图，不能单靠 issue 判断根因。应与 `#632/#629/#578/#619` 合并成 session / context / stream cluster 查。 | 先走 Phase 2 cluster triage，不单独修截图表象。 |
| `#632` 同项目多会话上下文膨胀、>100%、压缩跳变 | “>100% / 假百分比”已在本地确认；“跨 session”未由源码直接确认。新 session 创建时 `sdk_session_id=''`，历史消息读取按 `session_id` 过滤；但 project workspace / memory / dashboard 等共享上下文可能造成用户体感上的跨会话，需诊断面板解释 source。 | `src/components/chat/RunCockpit.tsx` 直接显示 raw `usage.ratio * 100`；`RunCockpitPopoverContent.tsx` 显示 percent 与 used/total；`ContextDotMatrix.tsx` unknown window fallback 到 200K。 |
| `#629` Claude Code Runtime session 恢复 400 + 空 assistant | Issue 正文指向坏 SDK session id / 空 assistant message；本地存在 compact 后必须清 `sdkSessionId` 的 contract，但还需行为测试覆盖 resume 失败、result 后 error、空 assistant 不落库。 | `src/lib/context-compressor.ts` 已有 `planStreamHandoffAfterCompaction()` 规则；Phase 2 要补失败路径与空 assistant guard。 |
| `#628` `@file` 变 `.codepilot-uploads` 副本，AI 误改副本 | 本地确认是实风险。`@file` 当前被转成 `FileAttachment`，不保留 origin path；chat route 保存用户消息时把所有非目录 files 写入 `.codepilot-uploads`，Claude prompt 后续引用的是上传副本路径。 | `src/components/chat/MessageInput.tsx` `fileResponseToAttachment()` 只保留 base64；`src/app/api/chat/route.ts` 写 `.codepilot-uploads`；`src/lib/claude-client.ts` 优先使用 `file.filePath`。Phase 3 必须拆 `attached_snapshot` / `editable_source`。 |
| `#633` Windows 11 installer 打不开 | GitHub 症状真实；本机 macOS 无法复现。release 资产只有 NSIS exe，无 portable zip fallback；workflow 只验证 exe 存在与 Electron ABI，不做安装启动 smoke / 安装日志采集。 | Phase 4 需要 Windows 真机或 CI 安装 smoke，并准备 portable fallback / 日志路径。 |
| `#626` 新版本提示导致 chat 页持续高 CPU | GitHub 症状真实；本地未确认根因。源码没有高频 update polling：browser fallback 是启动检查一次 + 8 小时间隔。可疑点转为渲染/动画/Markdown release notes/状态传播：`UpdateDialog` 使用 `ReactMarkdown` 渲染 release notes，ChatList / NavRail 在 update 可用时有 `animate-pulse` badge。 | Phase 4 必须用 CPU profiler / React profiler / performance sample 复现后再改；不要把根因写死为 polling。 |
| `#634` Native Runtime schedule/notify tools 不可用 | 当前源码探针不支持 issue 正文的“require notification 失败”根因：`getBuiltinTools()` 实际输出 `codepilot_notify / codepilot_schedule_task / codepilot_list_tasks / codepilot_cancel_task / codepilot_hatch_buddy`。此 issue 可能已被后续代码修掉，或问题在 UI 能力矩阵 / plan mode / prompt 编译 / 用户版本差异。 | `npx tsx` 探针已确认当前 registry 有工具。下一步应做 Native 真机 smoke：非 plan 会话里让模型创建 reminder；同时检查 Settings 能力显示是否误报。 |

### 2026-06-19 Claude Code 源码复核（Phase 1a，不采信 issue 根因，逐条对当前源码）

7 路并行复核当前 HEAD（`eed4640`，post-v0.56.0）。结论基本印证 Codex 表，并补出可直接落地的代码级证据；GitHub 侧 7 issue 均仍 open、无 label/milestone（#629 有 1 条 reporter 评论补充“SDK 进程被中断”第二症状）。

| Issue | 复核判定 | 关键证据（file:line） | Phase / 下一步 |
|-------|----------|----------------------|----------------|
| #632 | ">100%/假百分比" **确认是显示 bug**；"跨会话串" **源码不成立** | 未 clamp 渲染：`RunCockpit.tsx:169`、`RunCockpitPopoverContent.tsx:335`（`usage.ratio*100`）；`useContextUsage.ts:202` `ratio=used/contextWindow` 无 clamp；分母 `contextWindow` 仅在 SDK 上报时有（`claude-client.ts:259-261`），proxy 常缺 → 在真实/陈旧/200K fallback/null 间跳变；`ContextDotMatrix.tsx:57,88` 硬编码 200K。会话隔离正常：`db.ts:1247` 新 session `sdk_session_id=''`、`getMessages` 按 `session_id` 过滤。 | Phase 2：分母不可信时删百分比/remaining/unused，只留已用绝对值+kind 占比；有真实窗口才显示且 clamp≤100% |
| #629 | 空 assistant **已防护**；残留 **一个真实 gap** | 已防护：resume 抛错清坏 id 重试（`claude-client.ts:1531-1574`）、空内容不入库（`route.ts:1006`）、error-only 落可见 `**Error:**` 气泡（`route.ts:980-982`）。**Gap**：400 作为 `is_error` *result*（非抛错）时，`claude-client.ts:1908` 先置 `resultEmitted=true` 再到 `:1910-1921` is_error 分支，未清 `sdk_session_id`，且 `:2322` 的 crash 清理被 `!resultEmitted` 抑制 → 坏 id 留到下一轮。 | Phase 2：**先复现 400-坏session 的 error shape**（是抛错→已被 1569-1575 处理，还是 `is_error` result→看其 subtype/message），**再只对 session-state 类错误清** `sdk_session_id`（**不能无脑对所有 is_error 清**——限流/认证 is_error 会被误清 → 丢会话上下文，回归）；issue 重定向到 proxy-400 诊断 + MiMo proxy cluster（#577/#560/#563/#537）。详见决策日志 2026-06-20 Session/Stream 调研 |
| #635 | 截图-only 无可判根因；最强候选=**SDK runtime 无自生成 keepalive** | `STREAM_IDLE_TIMEOUT_MS=330s`（`stream-session-manager.ts:113`，`:386-392` abort）。Native 自心跳 15s（`agent-loop.ts:80,123-125`）→ 不会撞 idle；SDK 只转发不生成（`claude-client.ts:1956-1957`）→ 慢/排队 proxy 静默 >5.5min 被硬 abort（标 "Stream idle timeout — no response for 330s"）。即 tracker B-019/#499，未修。 | Phase 2 cluster：**liveness-aware heartbeat / timeout 设计**（**不要盲加 15s keepalive 去 reset idle 计时器——会掩盖真卡死**；心跳须反映上游真实 liveness，或换缓解方式：已知慢 proxy 拉长超时 / 探上游活性）；向 reporter 要 runtime/provider + 报错串确认。详见决策日志 2026-06-20 Session/Stream 调研 |
| #628 | **确认真实风险**，全链路核实 | origin path 在 `MessageInput.tsx:161-175`（`fileResponseToAttachment` 只留 base64）丢失；带 path 的 `body.mentions[]` 被送达但 `route.ts` **从不读**（`:42` 解构无 mentions）；mention file 与上传并入同一 `files[]`（`message-input-logic.ts:573-593`）→ 写 `.codepilot-uploads`（`route.ts:304-327`）→ prompt 引用副本路径（`claude-client.ts:1436-1440`）。AI 写操作命中副本。 | Phase 3：@-mention 带 originPath / 服务端消费 `body.mentions[].path`，in-tree 引用指真实路径、跳过 uploads 写 |
| #634 | 根因（catch 吞 require 失败）**不成立**——工具确实注册 | `builtin-tools/index.ts:250-258` `condition:'always'`；`notification.ts:48-352` 导出 5 工具；live 探针 `getBuiltinTools` 出 24 工具含全部 5 个 + 系统提示含 `codepilot_schedule_task`；wired 到 Native 模型调用 `agent-loop.ts:427-431`；contract test 451 通过。issue 引用的 "Not executable" UI 串**不在当前源码**（疑 pre-round-8 旧版）。**附带真问题**：Plan/safeReadOnly 下 `codepilot_list_tasks` 泄漏（`mutation-level.ts:71` 标 safe_read）。 | 大概率已修：需 Native 非-plan 真机 smoke + 问 reporter 版本；附带项另开小票；可在 `index.ts` catch 补 console.warn |
| #626 | polling **排除**；候选=**pulse 动画 × backdrop-filter** | `useUpdateChecker.ts:6` 8h 间隔 + 事件驱动（非高频）。`ChatListPanel.tsx:831` `hasUpdate` 时渲染 `animate-pulse`（永续 opacity 动画）徽章，位于 `globals.css:509-513` `backdrop-filter:blur(28px)` 侧栏内 → 每帧重采样/重模糊。切 Settings 换 `SettingsSidebar`（无 pulse）→ CPU 落，回来又起，精确对上症状。次要：`UpdateDialog.tsx:45-78` ReactMarkdown components 每次重建。 | Phase 4：profiler 先复现，再去 pulse / 移出 backdrop 层 / memo markdown |
| #633 | mac 无法复现；配置面=**NSIS-only + 未签名 + 无 portable + CI 不验安装** | `electron-builder.yml:64-67` 仅 nsis、无 portable/zip；0 Windows 签名配置（`after-sign.js:115` mac-only）→ 未签名 NSIS 被 SmartScreen/Defender 拦是头号假设（"转圈就没了"）；`build.yml:229-241` CI 只验 exe 存在 + better-sqlite3 ABI，**不跑安装/启动 smoke**；updater 已禁用（属全新安装失败非更新）。 | Phase 4：Windows CI 安装+启动 smoke + portable zip fallback + 签名/记录 SmartScreen 绕过；NEEDS-WINDOWS-REPRO |

复核教训沉淀：#634 是典型"issue 根因 + 引用 UI 串不可采信"——当前源码工具齐全、串不存在，只能归"疑旧版 + 需真机 smoke"；#629/#626/#635 的 issue 自述/直觉根因都不成立或不完整，真正可落地的是源码里更具体的 gap（is_error 不清 session id / pulse×backdrop / SDK 无 keepalive）。

治理面核验：

- `.github/workflows/build.yml` 当前只在 `workflow_dispatch` 与 `v*` tag push 触发；没有 `pull_request`，也没有 main push 的完整质量门。release CI 只跑 typecheck + 3 个 P0 pin，不跑完整 `npm run test` / smoke。
- `package.json` 已有 `typecheck`、`test:unit`、`test:smoke`、`test:e2e`、`test:visual`、`test` 脚本，远端门禁缺的是 workflow 编排，不是脚本能力。
- Phase 6 / Phase 7 的 GitHub 治理项应优先落地，否则 P0/P1 issue 仍会被无标签 feature PR 和 Vercel 噪音淹没。

## Phase 2 — Session / Context / Stream / Composer 可信主链路

> **进度（2026-06-20，Composer/Context 主链路 #1–#5 全部闭环，用户验证）：** 用户的 5 条 chat 主链路修复全部落地，#4/#5 经用户在真实桌面 App 重启后验证清空——
> **#1** 上下文用量 UI（P1 effective-base-URL gate `1e143d7` + 渲染期 provider gate `f5d4f15`；item 2「配置窗口为最高优先可信源」无真实数据源 → tech-debt #43；item 3 显示分母对齐压缩阈值另起阶段，源 = SDK `autoCompactThreshold`）、**#2** context-cost 非阻断（`19bf2dc`，info 提示无确认按钮）、**#3** Codex 图文按 Enter 发送（POC 探明格式 `9a9c5c5` + `buildCodexTurnInput` 接线 `1c870ce` + 真实 Codex 冒烟回复 `circle` 确认模型看图）。**#4/#5** 发送成功清空 / 失败保留草稿（✅ 闭环）：失败保留有 #615 基础（`composer-send-preservation.test.ts` 13/13）；发送成功后文本残留经 `58c7aad`（page.tsx accept 处清首条草稿）修复。**根因坑：用户残留 = 桌面 App 跑旧构建**——Electron dev 不可靠热更新 page 级文件、窗口刷新无效；Claude 协助完整重启 `electron:dev` 后，**用户在真实 App 验证发送即清空、确认闭环**（这也是之前「CDP 浏览器清、用户残留」矛盾的真因：浏览器硬刷过有修复、Electron 旧构建没有）。
> **待续：** Session/Stream cluster：#629（resume 400 / 空 assistant / 坏 sdk_session_id：**先复现 error shape，再只对 session-state 类错误清**，勿无脑清 is_error）+ #635（自动中断：**liveness-aware heartbeat/timeout 设计**，勿盲加 keepalive 掩盖真卡死）+ stream 终态原因码 + 压缩回滚 invariant + 诊断导出字段。

**用户能看到什么**：对话不再“半停半活”；resume 失败、context overflow、provider error 都有明确原因，坏 session 不继续污染下一轮；上下文用量不再用不可靠百分比吓人；图片等普通附件不会再弹出阻断式“确认发送”横幅；Codex 多模态输入能正常发送，发送后草稿清空。

**不做什么**：不重写 Runtime 架构；不把上游 Codex core 问题冒充 CodePilot 已修。

**怎么验收**：

- Codex Runtime 下：长 turn → Stop → 同会话下一条消息能被接受。
- Resume 400 / 失败不会产生空 assistant message，也不会继续沿用坏 SDK session id。
- Context source breakdown 能解释当前 thread / memory / attachments / summary / tool result 来源。
- 上下文总长度无法可靠取得时，任何 UI 都不显示百分比、剩余上下文或“未使用”数值；输入框右下角只显示当前已用上下文数值；浮层只显示已用上下文数值与不同上下文种类之间的占比。
- 图片 / 文件等用户主动添加的普通上下文，只能出现非阻断提示或用量说明，不能要求用户再点一次“确认发送”。阻断式确认只保留给真实危险动作：写文件、删文件、执行命令、提权，或把外部内容发送给模型 / 跨越权限边界的数据访问；单纯 Markdown / HTML / 网页预览不得二次确认阻断。
- 压缩失败时可回滚，不污染下一轮。
- Codex Runtime 下，文本 + 图片混合内容按 Enter 能发送（✅ #3 已达成）；发送成功后 composer draft / pending attachments / local echo 状态一致清空，失败时才保留可重试草稿（**✅ #4/#5 已达成——`58c7aad` 修复，用户在真实桌面 App 重启后验证清空；之前残留真因 = Electron 跑旧构建，见进度 note + 决策日志**）。

执行清单：

- [ ] 收口 `codex-stop-recovery.md` 的真实 smoke：Codex Runtime 登录态、长任务、Stop、同会话第二条。
- [ ] 对 `#635/#632/#629` 作为一个 cluster 处理：stream termination、session lock、SDK session id、context compression 一起 triage。
- [x] 修正上下文用量 UI 的语义：先核验 context denominator 是否真实可得；拿不到总窗口或 source 不可信时，删除百分比、剩余/未使用上下文显示，只保留“已使用”绝对值与 kind-level 构成比例。同步输入框右下角与上下文浮层，避免再出现 >100%。
- [x] 移除 context-cost 的发送阻断：当前证据指向 `src/lib/run-checkpoint.ts` 的 `context-cost-change`（`requiresConfirm`）+ `src/components/chat/MessageInput.tsx` 的 `blockingReasonIds` 拦截 + i18n `runCheckpoint.contextCost.*` 文案。修法方向：普通上下文过大只做非阻断提示 / tooltip / 状态说明，不进入 `blockingReasonIds`，图片发送不得要求二次确认。
- [x] 复现并修 Codex Runtime 多模态 composer 发送问题：图片 + 文本混合输入时 Enter 无效。重点核查 keydown/submit gate、pending attachments 状态、runtime capability 分支、空文本判断、IME/composition guard、Codex 附件序列化路径。
- [x] 复现并修发送后内容残留（**✅ 闭环，用户真实 App 验证**：`58c7aad` page.tsx accept 处清首条草稿。用户残留真因 = 桌面 App 跑旧构建（Electron dev 不可靠热更新 page 级文件、窗口刷新无效），Claude 协助完整重启 `electron:dev` 后用户验证发送即清空）：明确成功发送、失败重试、权限等待、stream abort 四种状态下 composer draft 清理规则；只有“未发送成功/需要用户重试”才保留草稿。
- [ ] 建立不可破坏 invariant：
  - 新 session 不继承旧 session 上下文，除非用户显式选择项目记忆 / 附件 / summary。
  - resume 失败必须清理坏 SDK session id 或降级到明确的 fresh-with-history。
  - stream 终态必须有原因码：user_stop / provider_error / network_error / context_overflow / runtime_crash / unknown。
  - context compression 失败必须可回滚。
  - 用户可见上下文数字必须有真实 source breadcrumb；没有可信 denominator 时不得显示百分比、remaining、unused。
  - 非破坏性、用户主动发起的发送动作不得因为估算型风险被二次确认阻断；估算风险只能降级为可忽略提示，除非会造成数据丢失、真实花费异常或权限提升。
  - Composer 清空必须以“消息已进入发送管线并被本地消息列表接管”为边界，不能因为 runtime 分支不同而漂移。
- [ ] 增加诊断导出字段：session id、provider id、SDK session id、runtime id、context source breakdown、last stream state、resume decision。
- [ ] 防回归：为 Stop / resume / compression failure / context UI semantic / context-cost 非阻断 / Codex 多模态发送 / draft 清理各加行为测试或 smoke 记录。

### 上下文窗口来源修订设计稿（2026-06-19，⚠️ 待 Codex 核实后再实现 #1/#3）

> 背景：用户反馈 GLM-5-turbo 仍显示总上下文 200K（"不是我们知道的部分"），要求窗口来源跨 Runtime、且只在可信时显示百分比。下方按"外部事实 / 本地证据(file:line) / 推断与设计"三层写，**请 Codex 逐条核实**，尤其打 ⚠️ 的不确定项。

**A. 外部事实 —— Claude Agent SDK `modelUsage.contextWindow`（已核实，来源见下）**

- 字段存在：`ModelUsage = { ..., contextWindow: number, maxOutputTokens: number }`，安装包 `@anthropic-ai/claude-agent-sdk@0.2.111` 的 `node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts:945`。
- **SDK reference 有字段定义，但来源/可信度未解释**（Codex 复核订正，原稿"官方文档不记载"不准确）：官方 Agent SDK **TypeScript reference 已列** `ModelUsage.contextWindow` / `maxOutputTokens`；但 **cost-tracking 指南**只列 input/output/cache/cost、**不解释这两个字段的来源与可信度**。即：字段有类型定义，语义/可信度无文档。参考：Agent SDK TypeScript reference + Cost tracking guide（2026-06-19 快照）。
- **来源 = SDK 内置模型目录，非 API 响应**：SDK 用 model id 查它捆绑的硬编码模型表得到窗口（claude-code-guide agent 从 minified cli 源 `contextWindow: ff(model, catalog())` 模式推断，⚠️ 推断非文档明述）。Anthropic Messages usage 本身不返回 context_window。
- 第一方 Anthropic 模型 → 可靠（表随 SDK 版本维护）；**第三方（自定义 model id 经 `ANTHROPIC_BASE_URL`，如 `glm-5-turbo`）→ SDK 表无此 id → 返回通用默认值**（⚠️ "默认值 vs 0" 系推断；本仓库**实测**返回 200000，见 B）。
- **无任何 SDK 设置 / 选项 / env 可配置或覆盖此值**（查 ClaudeAgentOptions / env / 文档，均无）。
- **工程结论（非 Anthropic 官方原文）**：第三方模型不应依赖此字段、宜自维护窗口来源。（Codex 复核：未核到 Anthropic 官方"自维护表"的明确表述，故降级为我们的结论，不署名 Anthropic 官方建议。）
- 核实方式：claude-code-guide agent 查 code.claude.com 官方文档 + 实测 installed `sdk.d.ts:945` + cost-tracking 文档（2026-06-19）。**请 Codex 复核 `sdk.d.ts:945` 字段是否存在，以及"SDK TypeScript reference 已列该字段、但 cost-tracking 指南未解释其来源/可信度"这一表述（已替代原稿"文档未记载"，与上方 A 第 2 条同步）。**

**B. 本地证据（repo facts，file:line）**

- `claude-client.ts:249-265`（`extractTokenUsage`）：从 `msg.modelUsage[key].contextWindow`（permissive 取键）写入 `token_usage.context_window`，`maxOutputTokens` 同理。
- **实测数据**（从用户真实 DB 查，session `0410798f…`，model `glm-5-turbo`，provider `aae782e0…`，mode code = Claude Code）：最近 assistant 消息 `token_usage = {"input_tokens":5825,"cache_read_input_tokens":52800,"context_window":200000,"max_output_tokens":32000,"usage_model_id":"glm-5-turbo",...}`。**注意 `context_window` 是 200000，≠ 本地 catalog 的 202752** → 证实它来自 SDK 而非本地 catalog。
- `model-context.ts:42`：catalog `glm-5-turbo = 202752`，注释自称引自 z.ai 文档。⚠️ **Codex 复核**：Z.AI GLM-5-Turbo 官方页实际写 **Context Length 200K / Max Output 128K，不是 202752** → 该 catalog 条目的"z.ai 核实"来源标注本身**存疑**。**因此不能声称 catalog 202752 比 SDK 200000 更准**（反而 SDK 的 200000 与 z.ai 的 200K 一致；但仍不能证明 SDK 的 200000 是 provider 实报而非 SDK 默认）。`getContextWindow` 对未知模型返回 `null`（无 200K 默认）。→ 另需单独核 `model-context.ts:42` 这条 catalog 的真实来源。
- `context-usage-walk.ts:100-126`：walk 把任意消息的 `token_usage.context_window` 当作 SDK 实报窗口（`latestSdkContextWindow`，newest-wins，会读历史旧值）。
- `useContextUsage.ts`：`contextWindowTrusted = sdkContextWindow != null || latestSdkContextWindow != null`（fix #1）→ 把上面 SDK 报的 200000 判为可信 → 显示。
- `agent-loop.ts`（Native）：catalog 洗白已删（`681706b`）——但那是 Native 路径；GLM 这个会话是 Claude Code，不经此路径，故 200K 仍在。
- 消费点 3 处：`RunCockpit.tsx:146`、`ChatView.tsx:345`、`ContextUsageIndicator.tsx:55`（后者全仓无 import = 死组件，可清理）。

**C. 诊断（推断）**

1. GLM 200000 = Claude Code SDK 对未识别模型的**通用默认值**，经 modelUsage 报回，CodePilot 照存照显示。非 GLM 接口返回、非本地 catalog、非用户设置、非 Native 洗白。
2. **fix #1 的"SDK 报的 = 可信"前提对第三方是错的**——第三方的 SDK contextWindow 是 SDK 内部值、不能证明是 provider 实报。⚠️ 但结论**只到此**，不延伸到"catalog 更准"：z.ai 写 200K、catalog 写 202752 且来源存疑（Codex 复核）。即"SDK 200000 不可作为 provider 实报采信"成立，"catalog 比 SDK 准"不成立。
3. >100%（#3）：显示分母（catalog/SDK）与**实际压缩阈值**（`route.ts:529` `getContextWindow||200000`）不是同一个数，且 walk 不感知压缩边界 → 压缩前 used≈阈值、分母偏小即冲过 100%（现已 clamp 表象，根因是分母不对齐）。

**D. 修订设计（提案，待核实后实现）**

- 窗口来源优先级（**Codex 复核统一口径，替代早前两处不一致写法**）：**用户手动覆盖 > provider/model 明确配置且来源已验证的 contextWindow（`provider-catalog.ts:62`）> 第一方 runtime 实报 > 只显示已用**。
- 第三方（Claude-compatible / 自定义 `ANTHROPIC_BASE_URL` / 自定义 base_url）场景下：**Agent SDK 的 contextWindow 默认不可信**。catalog 若无明确来源，**也不当"可信窗口"显示百分比**——最多用于内部压缩策略，UI 上要么只显示已用、要么明确标"估算/配置值"。
- 跨 Runtime：CodePilot 的表/覆盖对 Native / Codex / Claude Code 一致生效（解决用户"跨 Runtime"诉求）。
- #3：显示分母对齐压缩阈值，使百分比语义 = "距离压缩还剩"。
- 显示文案（用户已定）：可信窗口 → "百分比 + 已用"（如 56.6% 452K）；不可信 → 只已用；不单列"剩余"。

**E. 待定决策（请用户拍板）**

Codex 复核后统一为下面这条基线（不再是早前的 ①/② 二选一）：

> 用户手动覆盖 > provider/model 配置且**来源已验证** > 第一方 runtime 实报 > 只显示已用；第三方 SDK contextWindow 默认不可信，无明确来源的 catalog 不当可信%显示。

请用户定的剩余一项：第一方 Anthropic（无自定义 base_url）的 **SDK 实报窗口**是否自动当可信显示百分比——
- 选项①（推荐，对齐上面基线）：是。第一方自动显示百分比，第三方/无验证来源只显示已用。
- 选项②（更极端）：否。一律只在用户手动设了窗口时才显示百分比（含第一方）。纯手动、最可控，但第一方也要手动设一下。

**F. 给 Codex 的核实点**

1. ✅ Codex 已复核：`sdk.d.ts:945` 的 `ModelUsage.contextWindow` / `maxOutputTokens` 字段存在；SDK TypeScript reference **已列**这两个字段，只是 cost-tracking 指南**未解释其来源/可信度**（A 已按此订正，原"官方文档未记载"作废）。
2. SDK 对第三方 model id 返回通用默认值（⚠️ A 推断）—— 能否找到更硬的来源 / 或实测多个第三方确认默认值规律。
3. ✅ Codex 已复核：z.ai GLM-5-Turbo 官方写 200K context / 128K output（≠ catalog 202752）→ 仍需单独核 `model-context.ts:42` 这条 catalog 的真实来源（可能是错误引用）。
4. "第一方 vs 第三方"如何稳健判别（自定义 `ANTHROPIC_BASE_URL`？provider 类型？）——影响选项①的实现。
5. #3 压缩阈值对齐：`route.ts:529` 的阈值与显示分母确实脱节？

## Phase 3 — 文件引用与真实编辑路径安全

**用户能看到什么**：`@file` 给模型看时是只读快照；用户授权 AI 修改文件时，写操作回到真实源文件，不误改上传副本。

**不做什么**：不重做全文件系统权限模型；不让所有附件默认变成可编辑文件。

**怎么验收**：

- `@file` 引用能明确区分 `attached_snapshot` 与 `editable_source`。
- AI 写操作目标不能是 `.codepilot-uploads`，除非用户明确选择编辑上传副本。
- UI / tool result 能告诉用户“正在修改哪个真实路径”。

执行清单：

- [ ] 先核验 Notion 提到的 `#628` 当前状态与真实复现。
- [ ] 梳理现有附件、mention、file preview、write tool 的数据流。
- [ ] 设计两个语义：
  - `attached_snapshot`：模型上下文只读，可复制到 `.codepilot-uploads`。
  - `editable_source`：用户授权可修改的真实路径，必须保留 origin path 与权限确认。
- [ ] 写操作前加内部校验：目标路径若位于 `.codepilot-uploads`，必须有 explicit edit-upload-copy 意图。
- [ ] 补测试：mention file → write tool 目标路径、上传附件只读、路径安全、权限提示文案。

## Phase 4 — 安装 / 更新 / 日志 / 崩溃第一印象

**用户能看到什么**：安装失败能拿到日志；更新提示不拖慢聊天页；Codex 授权 / 搜索 / 网络路径不再把日志刷爆导致闪退。

**不做什么**：不自动发版；不手动创建 GitHub Release；不把 release 流程改成新体系。

**怎么验收**：

- Windows 安装路径有复现、日志与 fallback。
- 更新提示组件在聊天页无高频 polling / animation / rerender。
- `log-bloat-codex-runtime-crash.md` 的 live approval smoke 有记录。

执行清单：

- [ ] 核验 `#633` Windows 11 installer 当前状态：安装包类型、失败现象、是否已有 portable fallback。
- [ ] 核验 `#626` 更新提示高 CPU 当前状态：CPU 采样、触发页面、组件路径。
- [ ] 收口 `log-bloat-codex-runtime-crash.md` live smoke：真实 Codex Runtime、require_escalated / network approval、日志增长、RSS、crash breadcrumb。
- [ ] Release notes 加 stable / known issues / rollback 指引。
- [ ] 若 Windows installer 仍阻塞，准备 portable zip fallback 的最小 release 方案。

## Phase 5 — Electron / Preview / API / 权限安全硬化

**用户能看到什么**：大部分是无感安全，但错误提示更干净，权限超时不再静默；外部 Markdown / HTML / 本地网页预览像正常预览一样直接打开，JS 驱动的动态样式与交互能工作。

**不做什么**：不把 API key 加密落盘混入 Phase A/B 小修；该项若做，单独立项。不把 Preview 安全边界简化成“禁 JS / 只渲染纯 HTML”的能力阉割。

**怎么验收**：

- `codebase-health-audit-2026-06.md` Phase B 安全加固完成或明确拆分。
- 权限超时两个聊天入口表现一致。
- API 500 不再返回 stack。
- 外部 Markdown / HTML / 本地网页预览打开时不再弹多余确认。
- 本地 HTML / 网页预览允许页面自身 JS 驱动动态样式与交互，同时 sandbox / origin / filesystem boundary 清晰，不让预览绕过文件写入或权限边界。

执行清单：

- [ ] 完成 `codebase-health` A4/A5。
- [ ] 完成 Phase B 的 Electron IPC、terminal write、导出窗口、log sanitize、Sentry sanitize、install env、source map 核查。
- [ ] 移除外部 Markdown / HTML / web preview 的多余确认弹窗；普通预览打开不应要求再点一次“确定”。
- [ ] 打通外部本地 HTML / 网页 interactive preview：允许页面自身 JS 驱动动态样式与交互，而不是只保留纯 HTML；实现必须明确 sandbox / origin / filesystem boundary，不允许借预览绕过文件写入或权限边界。
- [ ] 对 Markdown / HTML / 本地网页 / 远程网页至少各留一个 smoke 或 Playwright 覆盖：打开无确认、JS 动态样式生效、控制台无阻断错误。
- [ ] 将 API key 加密落盘保持为单独 Tier 2 立项，先写方案，不混入当前小修 commit。

## Phase 6 — 测试、CI 与 release blocker

**用户能看到什么**：改坏核心链路更难漏到 release，稳定版更可信。

**不做什么**：不追求覆盖率数字；不把真实凭据 smoke 强塞进公共 CI。

**怎么验收**：

- PR CI 至少跑 typecheck + lint + unit。
- main CI 至少跑 typecheck + lint + unit + smoke。
- nightly 或手动 workflow 跑 e2e / visual / 跨平台 smoke。
- release blocker checklist 明确：P0 未关闭不能发 stable。

执行清单：

- [ ] 基于 `development-harness-optimization.md` 对齐现有 guardrail，不重复造轮子。
- [ ] 收口 `codebase-health` Phase C：
  - stream-session-manager 生命周期测试
  - destructive file routes 测试
  - interrupt 行为测试
  - E2E skip 分类与 composer/chat skip 清零或注明原因
  - context / structured-output / migration lock / OAuth 补洞
- [ ] 新增或调整 GitHub Actions：
  - `pull_request`: typecheck + lint + unit
  - `main`: typecheck + lint + unit + smoke
  - `nightly`: e2e + visual + cross-platform smoke
  - `release candidate`: 构建 preview / RC artifacts
- [ ] Release blocker checklist：
  - 无 open P0
  - v0.56.x milestone P1 有 owner / known issue
  - Codex Runtime Stop smoke 已登记
  - Windows installer smoke 已登记
  - file reference safety tests 已通过

## Phase 7 — GitHub Issues / PR 治理自动化 + 旧 issue 清库存

**用户能看到什么**：GitHub Issues 从“300+ 个低质量 / 过时 / 无标签条目堆在一起”变成几个明确桶：已修关闭、旧版本待复测、缺复现待补、功能请求停车场、P0/P1 当前主线。新 issue 进来后自动拿到基础分流，维护者不再手工判断每个“BUG / 无法使用 / 求支持”。

**不做什么**：
- 不让机器人自动关闭 P0 / P1 / 数据丢失 / 文件误改 / crash / auto-interrupt 类高风险 issue。
- 不把 issue 自述根因当事实；关闭“已修”必须有本地修复证据、release 版本或复测路径。
- 不用 LLM bot 直接改 label / close；第一批只做确定性 GitHub Actions + dry-run 审计脚本。
- 不一次性把所有 feature request 当作“已拒绝”；只把本轮稳定性无关项移出 v0.56.x 主线。

**怎么验收**：
- 线上 issue 首页不再是大批无 label / 无 milestone：新建 issue 自动分流，旧 issue 至少被分桶或关闭。
- `#628/#629` 等已修 issue 有修复说明并关闭；`#632/#635` 有当前状态评论；`#626/#633/#634` 留 open 但有明确 next step / needs-repro。
- 新增 workflow 不会自动关闭 P0/P1；`needs-repro` / `v0.57+ parking-lot` 的 stale 规则有 allowlist / dry-run 证据。
- PR 被自动打 `area:*`，大 PR 会收到 size warning，release blocker check 能在 open P0 时失败。

当前线上快照（2026-06-28 Codex `gh` 查询；2026-06-29 ClaudeCode dry-run 实测刷新）：
- open issue 实际 **399 个**（Codex 当时 `--limit 300` 打满、低估为"至少 300"）。
- 399 个 open 中 **392 个无 label、392 个无 milestone、208 个 >90 天未更新**（Codex 当时前 300 里见 293/293；实际规模更大）。
- v0.56.x milestone 与 9 个 P0/P1/needs-repro/parking-lot label 已存在；7 个核心 issue 已挂 milestone，但仍全为 open，其中 `#628/#629` 本地已修、需线上关闭。

### Phase 7A — 旧 issue 库存审计与分桶

**用户能看到什么**：旧 issue 不再全混在 open 列表里；维护者能先看“现在真的要修的 P0/P1”，而不是从几百条过时反馈里翻。

**执行清单**：

- [ ] 写本地 dry-run 脚本 `scripts/github-issue-backlog-audit.mjs`（只读 GitHub API，默认不改线上）：
  - 输入：open issues（至少前 500；如果不足则全量）。
  - 输出：`docs/research/github-issue-backlog-audit-YYYY-MM-DD.md` + JSON snapshot（可放 `docs/research/.generated/` 或脚本 stdout，不提交包含隐私日志的原文）。
  - 每个 issue 给建议桶：`fixed-close` / `old-version-confirmation` / `needs-repro` / `duplicate-cluster` / `feature-parking-lot` / `support-question-close` / `keep-p1-review`。
  - dry-run 报告必须列出：总 open、无 label、无 milestone、超过 30/60/90 天未更新、疑似老版本、疑似 feature、疑似重复标题。
- [ ] 第一批安全关闭（可直接执行，但必须逐条评论）：
  - `fixed-close`：本地已修、有 commit / 测试 / smoke 证据的 issue，例如 `#628/#629`。
  - 明显测试 / 垃圾 / support 已过期条目，例如标题为 webhook test、二维码过期且已有新渠道说明的条目（逐条确认，不靠标题批量硬关）。
- [ ] 第一批状态评论但不关闭：
  - `#632/#635`：评论当前修复状态、仍待项和用户可验证方式。
  - `#626/#633/#634`：保留 open，补 `needs-repro` / profiler / Windows repro 下一步。
- [ ] 旧版本批量策略：
  - 版本低于当前稳定线两个 minor 以上（如 0.30/0.37/0.49/0.50）且 45+ 天无更新、无日志、无可复现步骤 → 打 `old-version` + `needs-confirmation`，评论“请用最新版复测；14 天无回复将关闭”。
  - 第二轮（14 天后或同一批如果用户授权立即清）关闭 `old-version + needs-confirmation` 且无回复的 issue，关闭评论说明可用最新版信息重新打开 / 新建。
- [ ] 重复/同类聚合：
  - provider 连接失败、Claude CLI not found、模型列表缺失、bridge 失败、更新/安装类分别保留一个 canonical issue 或指向 `issue-tracker.md` 对应条目。
  - 重复项 comment 指向 canonical 后关闭，避免 20 个同类 open 条目分散维护。

### Phase 7B — 新 issue 保守机器人

**用户能看到什么**：新 issue 一进来就被要求补齐版本 / 系统 / runtime / provider / 日志；低质量 issue 不再长期沉底。

**执行清单**：

- [ ] 新增 / 调整 label：
  - `old-version` — 旧版本反馈，需最新版复测。
  - `needs-confirmation` — 等用户确认最新版是否仍复现。
  - `area:runtime` / `area:provider` / `area:context` / `area:file-reference` / `area:installer` / `area:bridge` / `area:ui` / `area:docs` / `area:tests` / `area:ci`。
  - `pr:large` — PR 过大，需要拆分或说明。
- [ ] 新增 `.github/workflows/issue-intake.yml`：
  - `issues.opened` / `issues.edited` 触发。
  - 根据 issue form 内容打基础 label：风险勾选“自动中断/崩溃”→ `P0-crash-or-interrupt`；“文件写入/数据丢失”→ `P0-data-loss-risk`；feature template → `enhancement` + `v0.57+ parking-lot`。
  - 缺版本 / OS / runtime / provider / 复现步骤时打 `needs-repro` 并评论补充模板。
  - 不自动关闭任何 issue。
- [ ] 新增 `.github/workflows/stale-needs-repro.yml`：
  - 只处理 `needs-repro`、`needs-confirmation`、`v0.57+ parking-lot`。
  - 明确排除所有 `P0-*` / `P1-*` / `P0-data-loss-risk` / `P0-crash-or-interrupt`。
  - 14 天无回复先提醒；再 14 天无回复才关闭。
  - 关闭评论必须说明“不是否认问题；请用最新版 + 日志重新打开 / 新建”。
- [ ] 更新 `.github/TRIAGE.md`：
  - 写清旧 issue 清库存口径、哪些能关、哪些不能关。
  - 写清“高风险不自动 stale”的硬规则。

### Phase 7C — PR 治理机器人

**用户能看到什么**：外部 PR 一眼知道影响面和风险，大 PR 不再无声进入 review 队列。

**执行清单**：

- [ ] 新增 `.github/labeler.yml` + `.github/workflows/pr-labeler.yml`：
  - `src/lib/runtime/**` / `src/lib/codex/**` / `src/lib/claude-client.ts` → `area:runtime`
  - `src/lib/db.ts` / migrations / schema types → `area:db`
  - `src/app/api/**` → `area:api`
  - `src/components/**` / `src/app/**` → `area:ui`
  - `electron/**` / main process / build scripts → `area:electron`
  - `docs/**` → `area:docs`
  - `src/__tests__/**` / `tests/**` → `area:tests`
  - `.github/**` → `area:ci`
- [ ] 新增 `.github/workflows/pr-size-warning.yml`：
  - 超过 800 changed lines 或 25 files → 打 `pr:large` 并评论要求拆分或说明。
  - 只 warning，不 fail。
- [ ] PR template 已存在，复核即可；不要复制 `CLAUDE.md` / `AGENTS.md` 大段规则。

### Phase 7D — Release blocker / milestone gate

**用户能看到什么**：发稳定版前不会漏掉 open P0；“能不能发”由一个明确检查回答。

**执行清单**：

- [ ] 新增 `.github/workflows/release-blocker-check.yml`：
  - `workflow_dispatch` + release/tag 前可手动运行。
  - 查询 `v0.56.x Stability / Trust` milestone。
  - 发现 open issue 带 `P0-crash-or-interrupt` 或 `P0-data-loss-risk` → fail。
  - P1 不 fail，但输出列表，要求 release notes known issues 或 owner。
- [ ] 在 `.github/TRIAGE.md` 写清：stable release 前必须跑该检查。

### Phase 7E — 执行顺序（给 Claude Code）

1. 先做线上只读 snapshot 和 dry-run 报告，不改 issue。
2. 建 label / workflow / triage 文档，提交代码与文档。
3. 跑 workflow syntax / `npm run lint:docs-drift` / `npm run test`（纯 GitHub workflow 可用 `actionlint` 若本地可用；不可用则记录未跑）。
4. 再执行第一批线上清理：
   - 关闭 `#628/#629`（已修证据充分）。
   - 评论 `#632/#635/#626/#633/#634` 当前状态。
   - 旧版本 / 低质量 issue 先打 label + 评论；是否立即批量关闭需用户确认，除非明显 fixed / duplicate / support 过期。
5. 回写：
   - 本计划 Phase 7 状态。
   - `issue-tracker.md` v0.56.x 表。
   - `.github/TRIAGE.md` 的实际 bot 行为。
   - Smoke Ledger / 决策日志记录 gh 操作数量：关闭 N、标记 N、评论 N、保留 P0/P1 N。

## Phase 8 — v0.56.x RC 与稳定版验收

**用户能看到什么**：稳定版发布前有 RC 和 known issues，用户知道该升级还是回滚。

**不做什么**：不自动发版；不绕过 release discipline。

**怎么验收**：

- `v0.56.x-rc.1` 或 preview artifact 给愿意试用者。
- Smoke Ledger 填完关键矩阵。
- Release Notes 明确更新内容、Downloads、Installation、Requirements、Changelog、Known Issues、Rollback。

Smoke matrix：

| 场景 | Runtime / 平台 | 验收 |
|------|----------------|------|
| Stop 后继续发 | Codex Runtime | 同会话第二条被接受 |
| Resume 失败 | Claude Code Runtime | 清坏 session id / 不产生空 assistant |
| Context 压缩失败 | 任一 Runtime | 有原因码且不污染下一轮 |
| `@file` 写操作 | 本地项目 | 写真实源文件，不写上传副本 |
| Windows 安装 | Windows 11 | installer 或 portable fallback 可用 |
| 更新提示 | macOS / Windows | chat page 无高 CPU |
| 外部 Markdown / HTML 预览 | Preview panel | 无多余确认；本地 HTML 的 JS 动态样式和交互生效 |
| Codex 授权 | Codex Runtime | 日志不暴涨，应用不闪退 |
| PR CI | GitHub | typecheck + lint + unit 生效 |

## Notion 建议映射表

| Notion 建议 | 本计划落点 | 备注 |
|-------------|------------|------|
| 冻结新功能，做 v0.56.x Stability | Phase 1 | 作为对外叙事与内部取舍原则 |
| `#635/#632/#629` 作为同一 bug cluster | Phase 2 | 必须重新核验 GitHub 当前状态 |
| `#628` 文件引用可能误改副本 | Phase 3 | 需要独立提级，不只当测试补洞 |
| `#633/#626` 安装与更新 CPU | Phase 4 | 第一印象与信任问题 |
| 日志 / 诊断 / crash breadcrumb | Phase 4 / Phase 5 | 已有 `log-bloat-codex-runtime-crash.md` |
| PR / Issue 治理 | Phase 7 | 包括模板、标签、triage 自动化 |
| CI 从发布打包升级为质量门禁 | Phase 6 | PR / main / nightly 分层 |
| 产品定位收束 | 全局原则 | v0.56.x 不扩新 feature surface |
| Commercial License / Support 页面 | v0.57+ 或稳定后 | 非本轮稳定性 blocker，先不混入 |

## Smoke Ledger

| Date | Runtime | Provider | Model | 凭据形态 | 场景 | Result | Evidence |
|------|---------|----------|-------|---------|------|--------|----------|
| _待填_ | | | | | | | |

## 决策日志

- 2026-06-19: 根据用户要求，将 Notion 文档中心的 GPT Pro 分析整理为 v0.56.x umbrella plan。核心取舍：先收束已开始的 codebase-health Phase A，再进入 Stability / Trust 主线；Notion issue 编号只作信号，执行前必须刷新 GitHub 当前状态与本地代码证据。
- 2026-06-19: 用户补充上下文 UI 与 composer 体验问题：上下文总窗口不可靠时不应显示百分比 / remaining / unused；图片等普通附件不应触发 context-cost 二次确认横幅。教训：不要把“系统估算的不确定性”转嫁成用户阻断；除非涉及破坏性操作、权限提升、真实异常花费或数据丢失风险，发送路径上的 guardrail 必须优先做非阻断提示。
- 2026-06-19: 用户补充 Preview 体验限制：外部 Markdown / HTML / 本地网页预览不应再弹“确定”式确认；外部本地 HTML / 网页预览应支持 JS 驱动的动态样式与交互。执行取舍：保留清晰 sandbox / origin / filesystem boundary，但不能用“禁 JS / 静态化”替代真实预览能力；看一眼文件不应被做成阻断式流程。
- 2026-06-19: **Phase 0 收束完成（Claude Code）。** codebase-health Phase A 提交为 `9c6e772`（A1–A6 主体）+ `f99edbb` / `d00449f`（A5 Step 2 权限超时侧栏徽章残留两轮 follow-up：先 useStreamSubscription、后收敛到 AppShell 全局 handler 以覆盖切走会话场景）。全程 typecheck + 全量单测 3360/3360。本 v0.56.x 计划文档单独提交（不混入 Phase A 修复 commit，按用户指示）。进入 Phase 1：先重新核验 #626/#628/#629/#632/#633/#634/#635（不采信 issue 根因，用当前源码/复现），再建 GitHub 治理入口（milestone / labels / issue+PR template / triage）。
- 2026-06-19: **Phase 1 完成**：milestone #1 + 9 labels + 7 issue triage（GitHub 在线）；`.github/` issue/PR 模板 + TRIAGE.md；7 issue 源码复核表写入本计划。Phase 2 #1（上下文百分比仅可信窗口显示 + clamp）`b842628` + mini-bar 去 200K `debd62a` + 显示格式"百分比+已用" `2c…`；#2（context-cost 非阻断发送）`19bf2dc`。
- 2026-06-28: **Phase 7 GitHub 治理批次细化（Codex）**。用户指出当前痛点不是模板本身，而是 open issues 大量低质量、过时且未关闭。Codex 线上核验：`gh issue list --state open --limit 300` 打满 300，前 300 个 open issue 中 293 个无 label、293 个无 milestone；v0.56.x milestone + 9 个治理 label 已存在，7 个核心 issue 已挂 milestone 但仍全 open（含本地已修的 `#628/#629`）。本次把 Phase 7 从“模板/label/自动化候选”改成可执行的一次性治理批次：旧 issue dry-run 审计与分桶、`#628/#629` 已修关闭、`#632/#635` 状态评论、`#626/#633/#634` 保留 open 且补 next step、`old-version`/`needs-confirmation` 标签、保守 stale bot（不碰 P0/P1）、issue intake、PR labeler、PR size warning、release blocker check。执行前必须先生成只读 snapshot / dry-run 报告，避免机器人批量误关高风险 issue。
- 2026-06-19: **Phase 2 context-window 来源修订设计（用户 review 驱动）。** 用户三点反馈促成更完整设计：① 上下文窗口不应限定 Claude Code，应跨 Runtime；② GLM-5-turbo 仍显示 200K（用户认为非真实已知）；③ >100% 可能含"对压缩感知不清"。**核验结论**：
  - **②根因（已修第一步）**：`agent-loop.ts:641-651`（Native）在无 context_window 时从静态 catalog 填 `totalUsage.context_window`，而 `context-usage-walk` + fix #1 把 `token_usage.context_window` 当作 SDK 实报（→ contextWindowTrusted），于是 catalog 猜测被"洗白"成可信 → Native GLM 显示 catalog 202752。**修法**：删掉该 catalog 洗白，Native 无实报时 context_window 留空 → useContextUsage 回落 catalog 为**不可信** → 只显示已用。注意：仅对**新 turn**生效（历史消息已存的 context_window 不改写，遵守不改用户数据）；旧会话需发新消息才转为已用-only。`CLAUDE_CODE_AUTO_COMPACT_WINDOW` 经核实 SDK 不回报、且仅 Claude Code，故不作为通用源。**⚠️ 后续核实更正**：用户实际看到 200K 的 GLM 会话是 **Claude Code runtime**，`token_usage.context_window=200000` 来自 **SDK 默认值**（非此 Native 洗白路径、非 catalog 202752）；Native 洗白修复（`681706b`）仍有效但治的是另一条路径。完整核实见上方「上下文窗口来源修订设计稿」B/C 段。
  - **①设计（待实现，Codex 复核统一口径）**：可信、跨 Runtime 的窗口源 = provider/模型配置且**来源已验证**的窗口（`provider-catalog.ts:62`，当前 useContextUsage 未消费）。优先级：**用户手动覆盖 > provider/model 配置且来源已验证 > 第一方 runtime 实报 > 只显示已用**；第三方 SDK contextWindow 默认不可信、无明确来源的 catalog 不当可信%显示（最多内部压缩用）。〔修正早前误写的"runtime 实报优先"——那会把第三方 SDK 值又洗白成可信、带回 GLM 200K〕。需把配置窗口流进 useContextUsage（3 消费点：RunCockpit / ChatView checkpoint / 死组件 ContextUsageIndicator 可清理）。
  - **③设计（待实现）**：显示分母与**实际压缩阈值**对齐（`route.ts:529` 的 `getContextWindow||200000` / `CLAUDE_CODE_AUTO_COMPACT_WINDOW`），使百分比语义 = "距离压缩还剩"；walk 当前不感知压缩边界，压缩触发前 used≈阈值，显示窗口偏小即冲过 100%（现已 clamp，但根因是分母不对齐）。
  - 显示文案（用户定）：可信窗口 → "百分比 + 已用"（如 56.6% 452K）；不可信 → 只已用；不单列"剩余"。
- 2026-06-19: **Codex 复核设计稿，订正后方可实现**。Codex 核为真：sdk.d.ts:945 字段存在、代码确实把 SDK contextWindow 写入 token_usage、useContextUsage 把历史 SDK 窗口当可信、Native 洗白已由 `681706b` 去掉、显示分母 vs 压缩阈值脱节成立。**订正 3 处**：① 原"官方文档不记载"不准（SDK TS reference 有字段定义，是 cost-tracking 指南未解释来源/可信度）；② 原"catalog 202752 比 SDK 200000 更准且 z.ai 核实"站不住（z.ai 官方写 200K/128K，catalog 该条来源存疑）；③ 设计 D 正文与决策日志 ①设计 的优先级冲突（决策日志旧写法把第三方 SDK 又洗白成可信）。**统一口径**（Codex 给）：用户手动覆盖 > provider/model 配置且来源已验证 > 第一方 runtime 实报 > 只显示已用；第三方 SDK contextWindow 默认不可信，无明确来源的 catalog 不当可信%显示（最多内部压缩用 / UI 标估算）。另"Anthropic 官方建议自维护表"未核到原文，已降级为工程结论。#1/#3 实现待此订正稿 + 用户决策后进行。
- 2026-06-20: **P1（env/legacy gate）+ item 1（存量会话渲染 gate）落地；item 2 前提被推翻 → tech-debt；item 3 已定位真实源但需独立接线。**
  - **P1 落地 `1e143d7`**：Codex 复审抓到 e42635d 的 first-party gate 只看 `resolved.provider?.base_url`；env/legacy/cc-switch（`resolved.provider=undefined`，`isFirstPartyAnthropicEndpoint(undefined)=true`）经 `settings.anthropic_base_url` / `process.env.ANTHROPIC_BASE_URL` 走第三方代理时仍把 SDK 默认 200000 写进 token_usage → GLM 200K 在该路径回归。修法：新增 `resolveEffectiveAnthropicBaseUrl(resolved)`（provider-resolver，忠实镜像 toClaudeCodeEnv precedence）作单一出口；claude-client compute-once 改用 effective base URL。行为测试 6 例（DB 第三方/官方/空 base_url + env process.env/settings 第三方/clean）+ doesNotMatch 回归守卫。同改 3 处反向误导注释（claude-client:250 / types context_window / model-context:36-42 标注 z.ai 实为 200K、202752 存疑）+ 文档 line 209。
  - **item 1 落地 `f5d4f15`**：服务端写入 gate 只保护新 turn；**存量** GLM 会话 token_usage 已存 200000，walk newest-wins 读历史旧值仍显假窗口。修法：providers/models route 给每个 group 标 `reportedContextWindowTrusted`（env 按 effective endpoint 第一方判；DB = `protocol !== 'anthropic' || isFirstPartyAnthropicEndpoint(base_url)`，**非 anthropic 协议如 Codex 保持 trusted**，避免误杀 `codex/runtime.ts:847` 写入的真实 modelContextWindow）→ ChatView 解析 active group（env-mode '' → 'env' 组）→ 两处 RunCockpit → useContextUsage 三分支 AND gate；untrusted → breakdown 不喂窗口 → 只显示已用。反例 smoke（curl /api/providers/models 对真实配置）：env=true、4 个 GLM DB 代理（含报 bug 的 `aae782e0…`）=false、openrouter=true、openai-oauth=undefined→默认 trusted。端到端 source-pin 入 `context-window-trusted.test.ts`。npm run test 3385/3385。
  - **item 2 前提不成立 → tech-debt #43**：line 449 / 设计 ① 假设可信源 = `provider-catalog.ts:62` 配置窗口。核实：`ProviderModelGroup.models[].contextWindow` 唯一来源是静态 catalog `getContextWindow`（route.ts:117/294/306 全由它填）；`provider_models` 无 context_window 列；presets 也未填（provider-catalog.ts:199 仅 zod schema）。即"配置窗口"= 已判 UNtrusted 的同一 catalog，升级为 trusted 会重引假数据。→ item 2 待真实 per-provider 窗口源（DB 列让 operator 设 / upstream 实报 / preset 核实填值）才能做，**不要把静态 catalog 当配置源**。详见 tech-debt-tracker #43。
  - **item 3 已定位真实源、需独立接线**：SDK 确有 `autoCompactThreshold` + `isAutoCompactEnabled`（`sdk.d.ts:2200-2201`，属 context-usage 报告对象），可支撑"距压缩还剩"语义；但该 snapshot producer 当前 dormant（claude-client 未调 `getContextUsage()`）。急性 >100% 已由 clamp + trusted gate 修掉。item 3 = 独立一阶段（先接 SDK context-usage 捕获 → 再按阈值显示），待用户排期。
- 2026-06-20（Phase 2 剩余三项核验，源码为准）：
  - **#4（发送成功清空）/ #5（失败留草稿）= 已有基础修复，但成功后文本残留仍需按用户实报复核**：此前 #615 工作已用 `composer-send-preservation.test.ts`（13/13 绿）锁定一批契约——成功 → PromptInput 自动清 text+files + MessageInput 显式清 inputValue/directoryRefs/badges；失败（provider gate / disabled / run-checkpoint / pre-accept POST 失败）→ `onSend` return false 或 `abortComposerSubmit()` 抛错 → PromptInput reject 分支保留 text+files；并含 first-message 的 isStreaming 延后翻 + keyed composer 防 remount 丢附件。**但 2026-06-20 用户再次明确：存在“内容已发出，但文本仍保留在输入框里”的体验问题。该症状不能被 #615 source-pin 直接视为已闭环，仍需单独复现并修：重点查 PromptInput 受控/非受控清空边界、`setInputValue('')` 与 `controller.textInput.clear()` 是否同源、成功 accepted 后 remount/local draft 恢复、badge/confirm/image 分支与普通 Enter 分支是否一致。**
  - **#3（Codex 图文按 Enter 发送）= 真 bug，根因已锁定，但实现需先 POC**。根因：`codex/runtime.ts:940` 的 `turn/start` input 写死 `[{ type:'text', text: options.prompt }]`，图片附件（`options.runtimeOptions.files` 里的 image）从不进入 input → Codex 只收到文本，图片被静默丢弃（Enter 能触发 submit、消息"发出"，但模型收不到图）。composer 层无 bug：`isSubmitEnabled` 对图文返回 true（`message-input-logic.ts:241`）、Enter handler 正常 requestSubmit。`codex/types.ts:153-156` 的 `CodexTurnStartParams.input` 也只类型化了 text。**阻塞点**：原生 app-server 的图片输入 wire format 仓库内无从确证——CodePilot 用裸 JSON-RPC（`CodexAppServerClient`）驱动 codex 二进制、**无 `@openai/codex-sdk` 源可读**；`proxy/translate-input.ts:208` 的 `{type:'image',image:url}` 是 OpenAI-compatible **proxy 路径**、非 app-server turn/start；`types.ts:132` 模型层声明 image 输入模态、但 turn/start 接受的块格式未知。POC 文件 `docs/research/codex-mcp-injection-poc/drive-codex-appserver.mjs` 仅示范 text turn/start。→ **下一步**：对真实 Codex app-server 做 POC 确认 turn/start 图片块格式（候选：`{type:'image',image:<path|dataUrl>}` / `{type:'input_image',image_url}` / 独立 images 参数），确认后再改 runtime.ts 拼 input 块 + 过滤 image vs 非 image 文件 + 跑真实 Codex smoke（Tier 2）。不可盲拼 wire format。
- 2026-06-20（#3 Codex 图文主链路收口 + Codex 冒烟）：`9a9c5c5` 先做隔离 POC，确认 app-server `turn/start` 合法图片块为 `{type:'image', url:<dataUrl|https>}` 或 `{type:'localImage', path:<absPath>}`，非法 `image_url` / `input_image` / `local_image` 均被 serde 拒绝；`1c870ce` 落地 `buildCodexTurnInput(prompt, files)` 并将 `codex/runtime.ts` 的 `turn/start.input` 从 text-only 改为读取 `runtimeOptions.files` 后拼 text + image/localImage（非图片文件维持跳过）。Guardrail：`codex-turn-input.test.ts` 11/11 绿，覆盖纯函数行为 + runtime source-pin 防回退 text-only；Claude Code 全量回报 3398/3398。Codex 复审补跑：`npx tsx --test --import ./src/__tests__/db-isolation.setup.ts src/__tests__/unit/codex-turn-input.test.ts` → 11/11；真实本地服务 smoke（`localhost:3000`，session `47c336c4cb5ca2884fede927afe9b99f`，`provider_id=codex_account`，`model=gpt-5.4-mini`，`runtime_pin=codex_runtime`，测试图 `/tmp/codepilot-codex-image-smoke/red-circle.png`）发送红色圆形 PNG + prompt "What basic geometric shape..."，DB 记录 user 消息含 `.codepilot-uploads/...red-circle.png`，assistant 回复 **`circle`** → 模型确实看见图片。项目 Playwright 冒烟：`npm run test:smoke` → 16/16。**#3 闭环。**
- 2026-06-20（Codex 复审 follow-up）：核心方向确认通过（DB-backed 第三方 anthropic 代理标 false、三分支 AND gate、Codex/非 anthropic 不误杀、item 2 推翻成立、3385/3385）。修 Codex 三点：**[P2]** `resolveEffectiveAnthropicBaseUrl` 在 `provider && !hasCredentials` 时原优先返回 `provider.base_url`，与 toClaudeCodeEnv（该状态两分支都不跑、SDK 只继承 ambient env）不一致 → 改成忠实镜像三态：有凭据→`provider.base_url`；无凭据→`process.env.ANTHROPIC_BASE_URL`（不读 provider.base_url、不读 settings）；无 provider→settings→env。加 2 例回归测试（无凭据 provider 背后第三方 env 仍 untrust / clean env 不误 untrust）。**[P3]** 存量第三方会话首屏可能在 provider models 加载前闪历史 200K：ChatView 改 **fail-closed**——`providerFetchState !== 'loaded'` 时传 `false`（宁可第一方会话短暂只显已用、% 后补的渐进式诚实显示，也不闪错数）；测试 pin 更新锁定该语义。**[P3]** tech-debt 编号 #36 与既有条目撞车 → 改为 #43。全量 3387/3387。
- 2026-06-20（#4/#5 首条消息文本残留收口）：用户再报“内容已发出但文本仍残留”。CDP 复现 + 埋点定位：ChatView 基本/图片发送在 accept 即清（`sendMessage` 早 resolve），唯独**首条消息（page.tsx）**残留整轮——(1) `sendFirstMessage` 内联 `while(await reader.read())` 消费整条 stream 才 resolve，composer 清在 `await onSend` 之后故晚；(2) 更关键，accept 时 `setIsStreaming` 翻转 hero→active 布局使 composer **remount**（#615 的 key 没挡住跨 ternary 分支切换——埋点 mounts++ / seen=[1,1] / fires=0 证实"signal 变化才清"的 in-component guard 被 remount 重置 ref 击穿）。remount 已重置 inputValue/附件/badges，唯一幸存的是 sessionStorage 草稿（inputValue 从它重读回刚发文本）。**修法 `58c7aad`**：page.tsx 在 accept 处 `sessionStorage.removeItem(composerDraftKey())`，remount 读空草稿即清；导出 `composerDraftKey` 作草稿 key 单一出口（MessageInput + page.tsx 共用）。撤回被击穿的 ComposerResetSignal/nonce 方案。Guardrail `composer-first-message-clear.test.ts`（draft-clear pin + doesNotMatch 防回退）。全量 3401/3401。CDP 实测真实 app：文本-only 首条 800ms（streaming 中）即清、图片+文本首条同样清、console 干净、ChatView 不受影响。**待 Codex 复审 + smoke。**
- 2026-06-20（#4/#5 **移交 Codex** —— 订正上一条「已修」）：上一条记 `58c7aad` 已修+CDP 验证，**但用户刷新后手测仍残留**，故订正为**未解决**。
  - **Claude 做了什么：** CDP 复现用「浏览器 localhost:3000 + 合成输入（native value setter + dispatch 'input'）+ press Enter」。结果：ChatView（GLM 会话）发送后 **250ms 即清**；新对话首条 pre-fix 残留、加 `58c7aad`（page.tsx 在 accept 处 `sessionStorage.removeItem(composerDraftKey())`）后 **800ms 清**。埋点证实首条会 remount（accept 时 setIsStreaming 翻转布局；mounts++/seen=[1,1]/fires=0，in-component nonce guard 被击穿），inputValue 从 sessionStorage 草稿重读 → 故改为清草稿。导出 `composerDraftKey` 单一出口。Guardrail `composer-first-message-clear.test.ts`，全量 3401/3401。
  - **矛盾：** 上述 CDP（浏览器 / 合成输入 / GLM+haiku）两路径都清，**但用户刷新后真实手测仍残留** → CDP 复现不能代表用户真实场景，不能算修复。
  - **Claude 未测 / 可疑变量（建议 Codex 重点查）：** ① **Electron app vs 浏览器**——用户很可能在 `npm run electron:dev` 桌面端测，Claude 只测了浏览器（同 dev server，但 Electron renderer / 缓存 / sessionStorage 行为可能不同）。② **真实 IME 中文输入**——Claude 用合成 input 事件，不触发 compositionstart/end；用户用中文 IME，composition 状态可能与清空/提交交互（`prompt-input.tsx` Enter handler 的 `isComposing` gate）不同。③ **provider/runtime**——Claude 测 GLM(ClaudeCode)+haiku，未覆盖用户实际 provider / Native / Codex。④ **症状定义**——需确认"残留"是「发送瞬间不清、整轮残留」/「永不清」/「文本回弹」中的哪种、以及是首条还是已有会话。
  - **建议路径：** 真实 Electron app + 真实键盘/中文 IME 复现；确认场景(首条 vs 已有 / provider)；判断 `58c7aad` 的 draft-clear 是否在用户实际路径上（若不是可 revert）。`58c7aad` 是真实的首条 clear-timing 改进（CDP 可证），但与用户残留是否同一问题待 Codex 判定。所有改动本地未 push。
  - **2026-06-20 二次收窄（用户确认场景 + 排除项）：** ① 用户答场景 = **「全新对话、空白框里自己打字」**（= draft 路径，**排除 Codex P1 的 prefill/initialValue 路径**——那条仍是 58c7aad 的真实漏洞、值得单独补，但不是本次用户残留）。② 浏览器 CDP 复现追加 **真实中文 IME composition 序列**（compositionstart → input(insertCompositionText,isComposing) → compositionend → input(insertText)）→ **仍 400ms 即清**（textarea="" + draft=null）。即合成输入 *和* IME 模拟在浏览器里都清。③ **结论：用户残留浏览器复现不到，强烈指向 Electron 桌面端 / 真实 OS 级输入法特定**（Electron renderer / sessionStorage / 真实 IME 与浏览器行为差异），**超出 Claude 浏览器-CDP 能力**。建议 Codex 直接在真实 `electron:dev` app 用真实键盘+中文输入法复现，并对比浏览器(localhost:3000)是否同样残留——若浏览器不残留、仅 Electron 残留，则根因在 Electron 层而非 React composer 逻辑。
- 2026-06-20（#4/#5 **闭环** —— 真因 = 桌面 App 旧构建，非 Electron 行为差异）：用户在浏览器实测**不残留**，确认仅桌面 App 残留 → 指向桌面端。Claude 协助完整重启 `electron:dev`（kill 整棵进程树：npm/concurrently/next dev/esbuild watch/electron + 释放端口 3000；后台重启、确认端口 + electron 进程拉起），**用户在重启后的真实 App 验证发送即清空 → #4/#5 闭环**。**根因：`58c7aad` 修复一直是对的；用户残留是 Electron dev 没把 page 级改动（`page.tsx`）热更新进 renderer，窗口刷新无效，得整进程重启。** 这也解释了全程「Claude CDP 浏览器清、用户残留」的矛盾：Claude 的浏览器是硬刷过的（有修复），用户的 Electron 是旧构建（没修复）——**不是 Electron 行为差异，是构建新鲜度差异**。教训：UI 修复在桌面 App 验证必须整重启 `electron:dev`，不能只靠窗口刷新；CDP 浏览器「已修」≠ 用户 Electron「已修」，差异可能只是旧 bundle。Codex P1（prefill/initialValue 回灌）仍是 `58c7aad` 的真实独立漏洞（input 来自 prefill 时 remount 从 initialValue 重灌），值得单独补，但与本次用户残留无关。
- 2026-06-20（#4/#5 收尾 — 补 Codex P2/P3 两个独立 gap，`a894473`）：**[P2] prefill 回灌**（即上条的 P1）：`/chat?prefill=...` 经 `initialValue` 进 composer，而 MessageInput 初始化 initialValue 优先于 draft；58c7aad 只清 draft 没清 prefill source → 带 prefill 进新聊天发送后、accept-remount 从 initialValue 重灌。修法：page.tsx 加 `consumedPrefill` 跟踪已发 prefill，accept 处 `setConsumedPrefill(prefillText)`，`initialValue` 改用 `effectivePrefill`（已消费→`''`、新 prefill 仍显示）。CDP 实测 `?prefill=预填测试XYZ`：预填正常显示，发送后 600ms 即清、URL 仍带 `?prefill` 也不回灌。**已知窄残余（Codex 复核，非 blocker）**：consumedPrefill 按文本内容记，同一 `/chat` 页面实例若再次收到**完全相同**的 `?prefill=...` 理论上会被当"已消费"而不显示；但常规任务页/快捷入口重新进新聊天会**重新挂载页面**（consumedPrefill 复位）→ 不触发。若以后要彻底消除，可改为按"发送计数/nonce"记消费、或 accept 处 `router.replace` 去掉 `?prefill`。**[P3] QuickActions 不守 #615**：onAction 原 `onSend(text); setInputValue('')` 无 await、不看 delivered → provider/model/目录没 ready 时 gated 仍清空。修法：改 `async` + `await onSend(text)` + 仅 `delivered !== false` 才清。Guardrail：composer-first-message-clear 加 P2 pin、composer-send-preservation 加 P3 pin，并把反复被 post-accept 新增撑爆的 #615「accept→setIsStreaming」距离 pin 改为顺序判断（indexOf）。全量 3403/3403。**#4/#5 及其衍生 gap 全部收口。**
- 2026-06-20（Session/Stream cluster 调研，源码核验，暂未改产品代码）：转 #629/#635，先对当前源码核验 gap + 识别设计风险。
  - **#629（坏 sdk_session_id / resume 400）= gap 属实，但安全实现需先 repro 形状。** 现状：resume **抛错**时已清 sdk_session_id（`claude-client.ts:1569-1575` catch → `updateSdkSessionId(sessionId,'')` + resumeFallback 通知）。**未覆盖**：turn 以 `is_error` **result**（无抛错）结束时（`:1934` `if(resultMsg.is_error)` 分支只发 status+Telegram 通知、**不清 sdk_session_id**），且 `:1932` 已置 `resultEmitted=true` → 后续 crash 清理被 `!resultEmitted` 抑制 → 坏 id 留到下一轮、下条消息重试坏 resume。**判别 nuance（关键）**：不能对所有 is_error 清——`RATE_LIMITED`/`AUTH_*` 等 transient 错误也走 is_error result，无脑清会强制开新 session、丢会话上下文（回归）。error-classifier 有 `SESSION_STATE_ERROR` 分类（patterns：'stale session'/'stale sdk_session'…）可作判别器，但 **is_error result 的 `subtype` 很可能是通用值（如 `error_during_execution`）、未必带 session 信号** → 需真实 repro 一个"400 坏 session 作为 is_error result"看其 subtype/message，才能写可靠判别（否则要么没修到、要么过度清）。**待办**：repro 该形状（类比 Codex 图片格式 POC）或确认它其实是抛错（→已处理）；确认后在 is_error result 分支按 `classifyError(...)==='SESSION_STATE_ERROR'` 条件清 + guardrail。
  - **#635（自动中断 / SDK 静默被硬 abort）= gap 属实，需 liveness-aware 设计。** 现状：`stream-session-manager.ts:113` `STREAM_IDLE_TIMEOUT_MS=330_000`、`:387` 超 330s 无事件即 abort；SDK 路径只转发上游事件、不自生成 keepalive（agent-loop 有 15s 自心跳，故不撞 idle）→ 慢/排队 proxy 静默 >5.5min 被硬 abort。**设计风险**：盲加 15s keepalive 去 reset idle 计时器会**掩盖真卡死**（真正 stuck 的流永不超时）。**待办**：keepalive 必须反映上游真实 liveness（而非盲计时），或换缓解方式（已知慢 proxy 拉长超时 / 探上游 TCP 活性）；需确认 agent-loop 15s 心跳是如何避免掩盖的、SDK 排队期有无可用的活性信号。
  - **结论**：两条均 Tier 2 session-recovery，安全实现各需一步前置（#629 repro / #635 liveness 设计），不盲改关键 stream 代码。
- 2026-06-26（#629 POC-A 源码层完成 + driver `--selftest` 5/5 绿，未改产品代码、未真实跑）：按上条「先 repro error shape」推进，落地 `docs/research/issue-629-resume-error-shape-poc/`（README 三层 + 可执行 driver `drive-resume-error-shape.mjs`）。**关键修正上一条的判别思路**：上条写"在 is_error result 分支按 `classifyError(...)==='SESSION_STATE_ERROR'` 条件清"——POC-A 核实 **`classifyError` 不吃 `result.subtype`**（入参 `ErrorContext.error`，`error-classifier.ts:123/370/379` 在 `rawMessage+stderr+extraDetail` 上匹配），且 **`SDKResultError.subtype` 仅 4 个通用枚举**（`error_during_execution`/`error_max_turns`/`error_max_budget_usd`/`error_max_structured_output_retries`，`sdk.d.ts:2715`，无 session 语义）→ **用 subtype 判别是死路**。**唯一潜在判别源 = `SDKResultError.errors: string[]`**（`sdk.d.ts:2725`），且 grep 确认 **claude-client 当前从不读 `errors[]`**（result event `1922-1932` 只透传 subtype/is_error/num_turns/duration_ms/session_id）。**修复设计因此精确化**：is_error 分支读 `errors[]` → `classifyError({error: errors.join('\n'), providerName, baseUrl})` ∈ {`RESUME_FAILED`/`SESSION_STATE_ERROR`} 才清 id、transient（限流/鉴权/预算）保留；`errors[]` 为空则退化用非文本旁证（resume turn 首个 result 即 is_error + 零 assistant 输出）；顺带把 `errors`/`stop_reason` 透传进 result event（对齐「诊断导出字段」待办）。**降险事实**：DB fallback（`buildFallbackContext`，`session-management-and-context-compaction.md:68`）兜底，清坏 id 后下轮用 DB 历史重建、大部分上下文仍在（降低误清 transient 的代价，但仍需判别、不无脑清）。**仍待 POC-B（真实凭据）定两个未知**：① 坏 resume 是 throw（→`1568` catch 已处理）还是 is_error result（gap）；② 若 result，`errors[]` 是否真含可判别 session 文本。driver `--selftest` 5/5 覆盖全 4 结局（throw / is_error+信号 / is_error+transient / is_error+空 errors / 意外 success），证**探测逻辑**正确——非 #629 已修。**第三方 proxy 最可能复现 400，跑 POC-B 需用户授权一个 provider 凭据**（脚本只从 env 读凭据、maskTail、`maxTurns:1`+`permissionMode:'plan'`、cwd 临时目录；坏 resume 在模型 turn 前失败 → ~零成本）。
- 2026-06-26（#629 POC-B 真实第三方 proxy 完成，Codex 只跑 driver + 回写文档，未改产品代码）：按上条 driver 对真实 Anthropic-compatible 第三方 proxy 跑坏 resume。**四个 provider 结论完全一致**：GLM CN (`https://open.bigmodel.cn/api/anthropic`, `glm-5-turbo`)、Xiaomi MiMo Token Plan (`https://token-plan-cn.xiaomimimo.com/anthropic`, `mimo-v2-pro`)、DeepSeek (`https://api.deepseek.com/anthropic`, `deepseek-v4-flash`)、Aliyun Bailian Token Plan (`https://token-plan.cn-beijing.maas.aliyuncs.com/apps/anthropic`, `glm-5`) 均返回 **`B_RESULT_ERROR`**，且 **第一条消息就是 `result`**（`subtype='error_during_execution'`, `is_error=true`, `num_turns=0`, `stop_reason=null`, `errors.length=1`）。`errors[0]` 原文稳定为 **`No conversation found with session ID: 00000000-0000-4000-8000-000000000629`**。结论：#629 对第三方 proxy 是真实 gap，不是已由 `1568` catch 覆盖的 throw path；`errors[]` 可作为精确判别源。**注意现有 classifier pattern 还差一个 wording**：`RESUME_FAILED` 有 `conversation not found` 与 `session id ... not found`，但都不匹配 `No conversation found with session ID: ...`（顺序相反/regex 形态不覆盖），修复需补 `no conversation found` 或 `/no conversation found.*session id/`。落地建议：Claude Code 在 `claude-client.ts` is_error result 分支读取 `resultMsg.errors ?? []` → `classifyError({ error: errors.join('\n'), providerName, baseUrl })`，分类为 `RESUME_FAILED`/`SESSION_STATE_ERROR` 才清 `sdk_session_id`；限流/鉴权/预算等 transient 保留；同时 result event 透传 `errors`/`stop_reason` 以补诊断字段。第一方 Anthropic 对照未跑（本机 env/DB 未发现可用第一方 key），不阻塞第三方 proxy 修复。
- 2026-06-26（#629 修复落地，Claude Code 实施，全绿）：据 Codex POC-B 实施。**改 3 点**：① `error-classifier.ts` `RESUME_FAILED` 补 `'no conversation found'`（baseline 实测真实文案改前 `UNKNOWN`、改后 `RESUME_FAILED`——现有 `'conversation not found'` 词序相反、session-id regex 要求 "not found" 在 id 后，均不命中）；新增纯函数 `isSessionStateResultError(errors, ctx)`（`errors.join('\n')` 喂 `classifyError`，仅 `RESUME_FAILED`/`SESSION_STATE_ERROR` → true，空/null → false）。② `claude-client.ts` is_error result 分支读 `resultMsg.errors`（cast，`SDKResultSuccess` 无 `errors[]`），`sessionId && isSessionStateResultError(...)` 才 `updateSdkSessionId('')`，transient（限流/鉴权/预算）保留——**正交于 #577**：#577 的 `!resultEmitted` gate 在 post-result catch（管成功 result 后噪音），本修复在 is_error result 分支内（管 result 本身是坏 resume），两者 source-pin 都在、不冲突。③ result event 透传 `errors`（仅 is_error 非空）/`stop_reason`，对齐「诊断导出字段」待办。**Guardrail** `issue-629-resume-session-clear.test.ts`（12 例：classifier 真实文案 → RESUME_FAILED + helper transient 不清的回归守卫 + claude-client wiring source-pin）。全量 `npm run test` **3415/3415**、`tsc` 0、`stream-result-error-guard`(#577) 仍绿。issue-tracker #629 → 🟢（待 Codex 复审 + 真实 resume-recovery smoke）。**仍待**：第一方对照（throw 路径本就被 `claude-client.ts:1568` catch 覆盖，非阻塞）+ 真实 app 端到端 smoke（坏 resume 后同会话下一条能发）。
- 2026-06-26（#629 Codex 复审 + 冒烟订正「已修」→ 补 route 持久化 P1/P2，全绿）：**上条 🟢 是 premature**——Codex 真实 route smoke（临时 DB）证明端到端仍复发。根因（Codex 抓）：claude-client 的 `updateSdkSessionId('')` 清了 DB，但 **result SSE 已带 `session_id` 先 enqueue**（`claude-client.ts:1931`），`/api/chat` 消费时 `route.ts:937` **无条件写回** `resultData.session_id` → 覆盖清理，第二轮同会话仍带坏 id 再报 `No conversation found`。**补 2 点**：**[P1]** `route.ts` result 分支改 gate——`resultData.is_error && isSessionStateResultError(resultData.errors)` 时 `updateSdkSessionId(sessionId, '')`（不写回坏 id），否则才写回 `resultData.session_id`（transient is_error 的有效 session_id 仍写回）。**[P2]** 同处 is_error 时用 `resultData.errors?.join('\n') || resultData.subtype` 设 `errorMessage`，让 empty-assistant guard（`route.ts:980`）落 `**Error:**` 气泡（否则失败首轮刷新像"没回答"）。route.ts import `isSessionStateResultError`。**Guardrail** 加 route.ts source-pin（P1 clear-not-writeback + P2 errorMessage 各一 pin；项目对 route 既定 source-pin + 真实 smoke 范式，见 `codex-proxy-error-visibility.test.ts`）。全量 `npm run test` **3418/3418**、tsc 0、`codex-proxy-error-visibility` / `stream-result-error-guard`(#577) 仍绿。**教训（memory `fix_all_consumers`）**：清 `sdk_session_id` 的 DB writer 不止 claude-client 一处，route 的 SSE 消费是另一个 writer——上轮只 sweep 了 claude-client 内部 retry path、漏了跨进程持久化层，premature-solved 第二次。issue-tracker #629 → 🟡（不再标 🟢），**待 Codex 复跑端到端 smoke**（两轮坏 resume → 第二轮 fresh、不再 `No conversation found`）才转 🟢。
- 2026-06-26（#629 关闭 🟢，Codex 端到端 smoke 通过）：Codex 复跑临时 DB route smoke 确认 P1/P2 端到端生效——第一轮坏 resume 后 DB `sdk_session_id` 被清空（不再被 route 写回坏 id）、失败 turn 落可见 `**Error:**` 气泡；第二轮同会话 fresh 起新 session、不再 `No conversation found`。三处状态同步转 🟢（issue-tracker / POC README / 本日志）。**#629 闭环**：error-classifier（补 `no conversation found` pattern + `isSessionStateResultError` helper）+ claude-client（is_error result 按 errors[] 判别清坏 id + 透传 errors/stop_reason）+ route（session-state is_error 不写回坏 id + 设 errorMessage）三层，guardrail `issue-629-resume-session-clear.test.ts`（全量 3418/3418）。第一方对照仍未跑（throw 路径本就被 `1568` catch 覆盖，非阻塞，不影响关闭）。
- 2026-06-26（#635 stream idle / liveness 调研完成 + 设计推荐，未改代码，待 review 后实现）：转 #635（SDK 慢 proxy 排队 >5.5min 被 idle abort）。后台 subagent 全面核验 + 主审 spot-check 2 处核心 claim。**根因 = SDK 排队期 app 层完全静默**：SDK keep_alive 被传输层 `Query.readMessages` `continue` 过滤、永不到 app 迭代器（`claude-client.ts:2002` 死代码，spot-check 确证 `sdk.mjs`）；`api_retry`（`sdk.d.ts:2022`）仅请求失败后发、且 claude-client `case 'system'` 不处理该 subtype 被丢；首 token 前无 `stream_event` → 客户端 330s idle（`stream-session-manager.ts:113/386-392`）abort。**Native 已掩盖真卡死**：`agent-loop.ts:123` 的 15s keepalive setInterval 在 try（`:161`）外、clearInterval 在 finally（`:699`），主逻辑卡死仍发心跳（spot-check 确证）；且 streamText 模型调用无超时兜底。**三方向**：A 盲 keepalive（解 #635 但掩盖真卡死，用户显式反对）；B liveness-aware（排队期零信号 → 静默场景不可实现）；**C 分级超时（推荐）**——首字节前用更长可配 `PRE_FIRST_TOKEN_IDLE_MS`（默认 ~600s，慢 proxy 合法排队）、首字节后维持较短 `POST_FIRST_TOKEN_IDLE_MS`，靠"是否已收到首个 model-output 事件"（`stream_event` delta / `assistant` / `result`，init/status 不算）翻标志区分；**不掩盖**（真卡死首字节前仍有真实上界、只是更长引信）。**折入 B 的 api_retry 接线**（`case 'system'` 加 `api_retry` → status SSE + markActive，覆盖重试风暴期）。UI 说真话（"等待服务商响应" / "流已停滞" / "上游重试中"），对齐「stream 终态原因码」待办。设计稿 `docs/research/issue-635-stream-idle-liveness-design.md`（三层 + 实现点 + 验收 + 明确不做，含 Native keepalive 掩盖 + streamText 无超时的 tech-debt）。**Tier 2 stream 核心，设计先行、待 review 后实现，不盲改。**
- 2026-06-26（#635 Codex review 通过方向 + 修正 P2/P3 边界，进入实现）：Codex 复核核心证据全部成立（keep_alive 死分支、api_retry 被丢、Native keepalive 在 try 外掩盖卡死、C 方向正确），**通过设计方向**；要求实现前修正两点，已落地设计稿。**[P2 事件定义]** `sawUpstreamModelOutput` 落到**前端实际 SSE 事件名**——算"首字节" = `text`/`thinking`/`tool_use`；**不算** = `status`/init、`tool_result`/`tool_output`、**`result`**（terminal / tool-call-only 首响会让两级语义变歪）；前端无直接 `assistant` SSE（assistant→`tool_use`、stream_event→`text`/`thinking`），故按 SSE 名而非 SDK message type 落地。**[P3 入口边界]** 330s abort 只在 `stream-session-manager.ts:386`（已有会话 ChatView）；首条 `/chat`（page.tsx `:886`）直接 fetch 自读 SSE、不走 idle checker → 首条本就不被 #635 误杀。**本期范围** = stream-session-manager 分级超时 + claude-client api_retry（防误杀核心，覆盖已有会话误杀面）；**首条一致 UI 文案 + 完整等待文案 = follow-up**（待用户定是否纳入验收）。进入实现。
- 2026-06-26（#635 核心实现落地，Claude Code，全量 3423/3423）：本期范围（防误杀核心）落地。**`stream-session-manager.ts`**：`STREAM_IDLE_TIMEOUT_MS` 拆两级——`STREAM_IDLE_PRE_FIRST_TOKEN_MS=600_000`（首字节前，慢 proxy 合法排队）/ `STREAM_IDLE_POST_FIRST_TOKEN_MS=330_000`（首字节后，维持原值，最小行为改动）；`ActiveStream` 加 `sawUpstreamModelOutput`，idle checker + idle 文案 `idleSecs` 按它选预算；**按 Codex P2** 仅 `onText`/`onThinking`/`onToolUse` 翻标志（= SSE `text`/`thinking`/`tool_use`），`onToolResult`/`onStatus`/result 不翻。**`claude-client.ts`**：`case 'system'` 加 `api_retry` → status SSE（前端 onStatus → markActive，重试风暴期不误 abort）；keep_alive 分支标注 DEAD（SDK 传输层 `readMessages` 已 `continue` 过滤）。**Guardrail** `issue-635-idle-tier.test.ts`（5 例 source-pin：两级常量 PRE>POST、idle checker 按标志选预算、**P2「恰好 3 处翻标志」负向 pin**、api_retry→status、keep_alive 标 dead）。typecheck 0、全量 3423/3423。**安全性**：改动保留真实上界（PRE 600s 后仍 abort），最坏只是把首字节前误杀阈值从 330s 提到 600s（设计意图），不会引入无限挂——故不阻塞发布。**follow-up**：完整 UI 等待文案 + 首条 `/chat`（page.tsx 不走 idle checker、本就不被误杀但无一致 UI）。issue-tracker #635 → 🟡。**待真实慢 proxy smoke**（首 token 前排队 6–9min 不被误 abort；真卡死仍在 PRE 后 abort）做正向确认。tech-debt（Native keepalive 掩盖卡死 + streamText 无超时兜底）见设计稿「明确不做」，未来单独立项或 Native 向 C 收敛。
- 2026-06-27（#635 Codex P2 修复——apiRetry status 不泄漏 raw JSON，全量 3425/3425）：Codex 复审实现，分级超时核心通过（PRE/POST、仅 text/thinking/tool_use 翻标志、首条已记 follow-up），但抓到 **api_retry 的 status SSE data 是 JSON（`{"apiRetry":true,"attempt":...}`），而前端 status 解析（`useSSEStream.ts:252` else / 首条 `page.tsx` else）会把不认识的 JSON 原样塞进 statusText → StreamingStatusBar 显示一段 raw JSON**——不是"没文案"，是回归。修法：两个入口各加 `statusData.apiRetry` 专门分支（在 raw-data fallback **之前**），显示人话 `Retrying upstream (attempt N)…`（跟随现有 statusText 英文硬编码风格如 "Connected"；完整 i18n 仍是单独 follow-up），useSSEStream 分支仍调 `onStatus` → markActive 刷新 idle（首条 page.tsx 不走 idle checker，故 display-only）。Guardrail 加 2 例 source-pin（两入口 apiRetry 分支存在 + 位于 raw fallback 之前，防 JSON 泄漏回归）。typecheck 0、全量 3425/3425。**待 Codex 复跑确认**。
- 2026-06-27（Phase 3 启动 — #628 @file 误改 `.codepilot-uploads` 副本，核心修复落地，全量 3436/3436）：转 Phase 3 文件引用安全。**根因（general-purpose agent 全链路调研 + 主审 spot-check 类型/注入终点）**：用户 @file 引用项目内真实文件，但 `fileResponseToAttachment`（MessageInput.tsx:173）把 mention 转成 base64-only FileAttachment、丢真实路径；`route.ts` 把所有非目录 `files[]` 写进 `.codepilot-uploads/` 副本；`claude-client.ts:1455-1461` 把副本路径注入 prompt + "用 Read 工具读" → AI 读/Edit 副本 → 用户真实文件没变。前端虽发 `body.mentions[]`（含真实 path）但 `route.ts` 从不读（grep 确认）。**修法（模仿 directory 已有的 filePath skip-copy 模式）**：① `FileAttachment` 加 `originPath?`（types）；② mention 在 MessageInput `fileResponseToAttachment(res, filename, 'mention', safePath)` 保留真实相对路径；③ `route.ts` files.map 加分支 `resolveInTreeAttachmentPath(f.originPath, workDir)` 校验 cwd 内真实文件 → filePath = 真实绝对路径、跳过 copy，否则降级写 copy；④ claude-client / getUploadedFilePaths / fileAttachments map **无需改**（filePath 现指真实路径 → prompt 引真实路径 → AI Edit 落真实文件）。**安全（关键）**：新增纯函数 `src/lib/in-tree-attachment.ts`，**绝不信任 client 路径**——`path.resolve(workDir, originPath)` + containment（`=== base || startsWith(base + sep)`，防 prefix-sibling `workDir-evil` 逃逸）+ isFile；out-of-cwd / 缺失 → null → 降级 copy（非破坏）。**这不是新增破坏性**：@file 引用项目文件本就预期"AI 改我的文件"，当前"改副本"才是 bug；修复恢复用户预期，且 `enableFileCheckpointing` 已 plumbed 给 undo。**Guardrail** `issue-628-mention-real-path.test.ts`（11 例：安全函数真测含 ../逃逸 / 绝对路径 / prefix-sibling / 目录 / 不存在 + 链路 source-pin）。typecheck 0、全量 3436/3436。**待 Codex 复审 + 真机 smoke**（@file 引用项目文件 → AI Edit → 真实文件变更、git diff 可见）。**follow-up**：Codex runtime `buildCodexTurnInput` 当前只接图片、非图片 mention 文件不进 input（相关 gap，agent 提示）；Phase 3 的 @file 只读快照 vs 可编辑源文件语义分离仍待续。
- 2026-06-27（#628 Codex P1 修复——上条的字符串级 containment 不挡 symlink，改用 realpath 口径，全量 3438/3438）：Codex 复审抓到上条 `resolveInTreeAttachmentPath` 只做**字符串级** `path.resolve` containment + `statSync`（跟随 symlink）——in-tree symlink `linked-secret.txt -> ../outside` 字符串看在 cwd 内、statSync 看到 regular file → 被放行，AI Edit 跟 symlink 写到项目外（Codex 本地复现：返回 workDir 内 symlink 路径、realpath 出 workDir）。**修法（复用现有 contract，不自写 realpath 校验）**：`resolveInTreeAttachmentPath` 改 async，复用 `src/lib/files.ts` 的 `assertRealPathInBase(candidate, workDir, { rejectIfSymlink: true })`（`fs.realpath` 双侧 + `lstat` 拒 final-component symlink + `isPathSafe` containment），返回 realpath 物理路径后再 `statSync(...).isFile()`；path_unsafe / symlink_detected / not_found → catch → null → 降级 copy（非破坏）。`route.ts` files.map 随之改 `await Promise.all`。**此口径替代上条的字符串级 containment 描述**（prefix-sibling 仍被 isPathSafe 覆盖）。**Guardrail** 加 3 例：Codex 复现的 in-tree symlink 逃逸 cwd → null、in-tree symlink 指向 cwd 内也 → null（rejectIfSymlink）、source-pin「复用 assertRealPathInBase」。typecheck 0、全量 3438/3438。#628 仍 🟡，**待真机 smoke**（@file → AI Edit → git diff 真实文件变更）。教训：路径安全的"在不在目录内"必须 realpath 解析再判，字符串级 resolve + statSync 会被 symlink 击穿——且项目已有 `assertRealPathInBase` 口径，应直接复用而非自写。
- 2026-06-28（#628 关闭 🟢，Codex 真机 smoke 通过）：Codex 真机 smoke 验证 @file → AI Edit → git diff，确认编辑落到用户真实文件（不再是 `.codepilot-uploads` 副本）、symlink 文件走降级不逃逸；P3 测试可移植性（symlink fixture 测试内创建 + EPERM skip，`b709134`）也已收。**#628 闭环**：types `originPath` + MessageInput 保留真实路径 + route 复用 `assertRealPathInBase(rejectIfSymlink)` 校验后引真实路径 + claude-client 自动落真实文件，guardrail `issue-628-mention-real-path.test.ts` 13/13。四处状态转 🟢（issue-tracker / Phase 3 状态总览 / 顶部状态行 / README 索引）。**Phase 3 主问题关闭**；剩 follow-up：Codex runtime `buildCodexTurnInput` 非图片 @file 不进 input（语义 gap）、`@file` 只读快照 vs 可编辑源语义分离。commit 链 `f6f05c2` → `1380d91` → `b709134`。
- 2026-06-29（Phase 7A 第一批执行 — dry-run 审计 + 核心 issue 线上清理，ClaudeCode）：(1) **只读 dry-run 审计**：新增 `scripts/github-issue-backlog-audit.mjs`（commit `f72bb17`）+ 用户 review 返工（`sanitizeTitle` 标题归一化修标题内换行拆表格 / 聚类只按标题命中 / 去 canonical 自动推荐，commit `f1d38e3`）；产出 `docs/research/github-issue-backlog-audit-2026-06-29.md`：399 open issue 分桶（keep-p1-review 5 / fixed-close 2 / old-version 54 / needs-repro 170 / feature 55 / support 15 / uncategorized 98；392 无 label、208 个 >90 天未更新）。脚本只读、绝不写 GitHub、报告不含 issue 正文。(2) **线上第一批（用户授权 gh 操作）**：关闭 2（#628/#629，已 v0.56.2 修复，带说明评论指向升级验证）+ 评论 5（#632/#635 当前状态、#626/#633/#634 保留 open 补 next step / needs-repro）+ 保留 P0/P1 5（即这 5 个，未自动处理）。**未做**：7B/C/D 保守机器人（含 stale bot 产品取向，待用户定）、54 旧版本 / 98 待分类批量处理（量大、涉及批量评论用户，待用户定方向）。所有 commit 本地未 push。
- 2026-06-29（Phase 7B 保守机器人 + 存量打标签，ClaudeCode）：(1) **stale 机器人** `.github/workflows/stale-needs-repro.yml`（actions/stale@v9）：带 `needs-repro`/`needs-confirmation` 的 issue 14 天无更新自动评论、再 14 天关闭（带可重开说明）；豁免全部 P0/P1、不碰 feature（parking-lot 不在 any-of-labels）、不动 PR、每次限 60 条。新建 3 label：old-version / needs-confirmation / stale。**⚠️ 需 push 到 GitHub 才生效**（本地 commit 不触发 Actions）。(2) **存量打标签**（gh 线上，加 label 不通知报告者）：19 旧版本 bug（≤0.54 版本号、≥90 天无更新、spot-check 过、排除误入 feature #210/#371）→ old-version + needs-confirmation（push 后由 stale 接管评论+超时关）；79 feature → v0.57+ parking-lot（不 stale、长期保留）。(3) **方案改进（反假数据）**：用户原要"直接关重构前老 issue"，但 dry-run 实测重构前 367 个里 62 是功能需求、277 疑似 bug 混着仍相关的（#578 是 CLAUDE.md 在跟踪的 Codex 中断问题），故改"标签 + stale 温和流程"，不按创建日期一刀切。**未做**：needs-repro 170（dry-run 启发式不够可靠，建议靠 issue-intake bot 管新增 + 存量逐步，不批量 stale 关）、issue-intake / PR labeler / release-blocker workflow（7B 剩余 / 7C / 7D）。所有 commit 本地未 push。
- 2026-06-29（Phase 7B stale 激活 + issue-intake + 存量校准，ClaudeCode，应 Codex 建议）：(1) **stale 已 push 激活**（commit 78acefe push，GitHub 确认 workflow active）；首次运行 0 影响（19 个 needs-confirmation 当天打标签、3 个 needs-repro 均 <14 天，无 ≥14 天符合项）→ 平滑启动，约 7/13 首次提醒、7/27 起按规则关。(2) **issue-intake 机器人** `.github/workflows/issue-intake.yml`（github-script，issues.opened/edited，只作用新 issue）：risk checkbox→P0、bug 缺复现→needs-repro+一次性评论、编辑补全→自动摘 needs-repro；feature→parking-lot 为冗余兜底（feature 模板 `feature_request.yml` 已自带 enhancement+parking-lot label）。**需 push 才生效**。(3) **存量 needs-repro 校准（抽 20，反假数据）**：dry-run 的 needs-repro 启发式**误判率约一半**——带截图/详细描述但无"复现/版本"关键词的真 bug 被误判（#578 中断 / #545 模型自动变 / #544 API 报错 / #549 小米连接等），另混入垃圾（#555「G」/ #548 webhook test / #562 求进群）与 feature（#572/#547）。**结论：存量 170 绝不批量打 needs-repro + stale 关**（会误伤大量有信息真 bug）。**决定**：存量靠人工逐步 triage，新增靠 intake（form 结构化、准）止血；Codex"先止血再清旧账"经实测验证正确。**未做**：PR labeler / size-warning（7C）、release-blocker（7D）。
