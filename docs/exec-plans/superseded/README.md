# Superseded Plans / 已被接管计划

被更新的计划（如 `refactor-closeout`）接管、仅作**历史参考**的旧执行计划。

**这些不是当前任务入口** —— Claude Code / Codex 不应从这里领取工作；当前工作以接管它的计划或 `active/` 为准。

- 每份文件顶部有 `Archive note`，记录被谁接管、为什么移出 `active/`、未来若重启从哪里开始。
- 索引见 [../README.md](../README.md) 的「Superseded（被接管，历史参考）」表。
- 重启方式：由用户主动发起，确认接管关系解除后再 `git mv` 回 `active/`。
