# i18n Guardrail

> **Status: Stub** — 七节模板占位。首次真实改动触发时由实施 Agent 按 [`README.md`](./README.md) 的七节模板填充。
> **为什么先读**：UI 文案改动必须同步 `en.ts` 和 `zh.ts`；漏一边会让某种语言下功能失语（AGENTS.md 改动自查第 1 条）。
> **已知关键文件**：`src/i18n/en.ts`、`src/i18n/zh.ts`、`src/lib/i18n/*`。

## 词汇表

（待填充）

## 不变量 / 契约表

| # | 不变量 | 由谁守 |
|---|--------|--------|
| 1 | （待填充——示例：每个 i18n key 必须在 en.ts 和 zh.ts 中都有定义，缺失视为 build 错） | |

## 关键文件 + 责任

| 文件 | 守哪条不变量 |
|------|--------------|
| `src/i18n/en.ts` | 英文文案 source of truth |
| `src/i18n/zh.ts` | 中文文案 source of truth |
| `src/lib/i18n/*` | 运行时 key 解析与 fallback 策略 |

## 改动检查表

- [ ] 新增 i18n key 时同时在 en.ts 和 zh.ts 添加
- [ ] 删除 i18n key 时两边同步
- [ ] 修改文案语义时考虑中英文是否仍然对应同一含义
- [ ] （待填充）

## 常见坑

- （待填充——参考 `feedback_describe_in_user_terms.md`：与用户讨论 UI 时用日常话，不用变量名 / 文件路径）

## 测试覆盖

| 契约 | 测试文件 |
|------|----------|
| | （待填充） |

## 设计决策日志

- YYYY-MM-DD — 首次真实改动时记入第一条
