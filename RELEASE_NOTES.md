## CodePilot v0.67.1-y.1（Fork 版）

> 基于上游 CodePilot v0.67.1 的个人 fork 构建。本版让「自动批准」贯通到新会话默认权限，新增 composer 用量入口，并修复用量统计页日期显示。

### 本版改进（Fork）

- **自动批准贯通新会话** — 通用设置里「自动批准所有操作」开着时，新建会话的权限直接默认「完全访问」，composer 的权限标记与实际执行一致，不再出现「显示需要询问、实则已放行」的错位（仍可在会话内手动切换，手选不会被覆盖）。
- **composer 用量入口** — 会话 token 统计右侧新增「用量」入口，点开可看订阅额度：Codex 实时拉取配额；Claude Code 展示最近一次流式回复上报的用量窗口，没有数据时显示诚实空态而非假 0。
- **用量统计页日期修复** — 设置 → 用量统计的「每日费用」「每日 Token 用量」两张图，x 轴日期改为统一补零格式、两图对齐、密度稳定，不再出现日期缺失或两图对不上的情况。

### 承接前版功能（Fork）

- 会话列表：hover 直接出「分屏 / 归档」按钮；右键完整操作菜单（分屏 / 重命名 / 复制 ID / 已读会话 / 归档 / 删除会话）。
- 未读提示：后台会话跑完用蓝点 + 加粗标题标出，打开或右键「已读会话」即清除，不改变排序。
- 彻底删除会话：右键「删除会话」为物理删除（区别于「归档」软删除），弹确认框，删除后不可恢复。
- 更新下载可暂停 / 继续（断点续传）/ 取消；下载进度上报优化；下载中检查更新不再假死。
- 归档代替删除对话（软删除，数据保留在本地、从列表隐藏）。
- 更新下载走系统代理（Clash / VPN），检查更新指向本 fork 的 Releases。
- 可关闭的 Provider 密钥本地加密（设置 → 通用；全新安装默认关）。
- 更新装完自动退出；会话列表 ≥7 天时间显示为紧凑日期。
- 应用内网络代理；Claude Code CLI 登录作为 Provider；默认面板与已完成工具默认折叠。

### 说明

- 本 fork 只发布 **macOS Apple Silicon（arm64）** 构建。
- 构建为 ad-hoc 签名、未 notarize：首次打开若提示应用「已损坏 / 无法验证开发者」，右键点按选择「打开」，或执行 `xattr -dr com.apple.quarantine /Applications/CodePilot.app`。

### 上游基线

完整的上游 v0.67.1 更新内容见上游仓库 op7418/CodePilot 的 v0.67.1 Release。
