## CodePilot v0.67.1-y.3（Fork 版）

> 基于上游 CodePilot v0.67.1 的个人 fork 构建。本版新增「编辑并重新发送最后一条消息」——改完最后一句提问就能截断旧回复、用新内容重跑一轮。

### 本版改进（Fork）

- **编辑最后一条消息并重新发送** — 会话不在流式中时，最后一条纯文本提问 hover 出现「编辑」按钮：
  - 就地变输入框修改（⌘/Ctrl+Enter 保存并重发，Esc 取消）；保存后会删掉这一轮旧提问与它的回复，用新内容重跑一轮。
  - 底层等价于「截断 + 重发」：后端清空 SDK 会话，让模型上下文从截断后的真实历史重建，不残留被删掉那一轮。
  - 仅支持 **Claude Code / 本地 Runtime**；Codex 会话暂不提供编辑入口（其历史在服务端 thread，尚无法干净回滚，列为后续）。
  - 带附件的消息不提供编辑入口，避免重发时丢附件造成语义错位。

### 承接前版功能（Fork）

- 会话用量按来源实时查询：composer「用量」按当前会话 Runtime + 服务商只显示适用的那一个（Claude 订阅 / Codex 配额 / 自带 Key 诚实空态），失败或未登录显示空态、不显示假 0。
- 自动批准贯通新会话：开启「自动批准所有操作」时，新建会话默认「完全访问」，权限标记与实际执行一致（手选不被覆盖）。
- 用量统计页日期修复：设置 → 用量统计两张图 x 轴日期统一补零、两图对齐、密度稳定。
- 会话列表：hover 直接出「分屏 / 归档」按钮；右键完整操作菜单；未读蓝点标记后台跑完的会话；「删除会话」物理删除（区别于归档软删）。
- 更新下载可暂停 / 继续（断点续传）/ 取消；走系统代理（Clash / VPN）；装完自动退出。
- 可关闭的 Provider 密钥本地加密（设置 → 通用；全新安装默认关）。
- 应用内网络代理；Claude Code CLI 登录作为 Provider；默认面板与已完成工具默认折叠。

### 说明

- 本 fork 只发布 **macOS Apple Silicon（arm64）** 构建。
- 构建为 ad-hoc 签名、未 notarize：首次打开若提示应用「已损坏 / 无法验证开发者」，右键点按选择「打开」，或执行 `xattr -dr com.apple.quarantine /Applications/CodePilot.app`。
- 会话「用量」的 Claude 侧依赖系统 `curl`（macOS 默认自带）；缺失或未以 claude.ai 订阅登录时显示空态。

### 上游基线

完整的上游 v0.67.1 更新内容见上游仓库 op7418/CodePilot 的 v0.67.1 Release。
